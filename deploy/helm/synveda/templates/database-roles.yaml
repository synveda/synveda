apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "synveda.fullname" . }}-database-roles
  labels:
    {{- include "synveda.labels" . | nindent 4 }}
    app.kubernetes.io/component: database-contract
data:
  roles.json: |
    {{ include "synveda.databaseRolesJson" . }}
