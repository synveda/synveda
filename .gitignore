/target/
# The gateway's Tantivy sidecar (CTX-1): SYNVEDA_SEARCH_INDEX_DIR defaults
# here, so running the binary or a demo from the repo root materialises a
# per-tenant index. Derived data, rebuilt by the indexer's sweep.
/data/
# Written by `synveda init` (OPS-1): this deployment's tenant id, issuer and
# embedder. Generated, per-machine, and regenerated by re-running init —
# committing it would hand every checkout one laptop's tenant.
/deploy/compose/.env
/deploy/compose/.env.local
/deploy/compose/runtime/
/deploy/compose/secrets/
/deploy/compose/backups/

node_modules/
.pnpm-store/
dist/
# The console's Node-side test output (CNSL-1): the pure modules compiled
# for `node --test`, separate from the Vite bundle in dist/.
dist-test/
*.tsbuildinfo
.DS_Store
Thumbs.db

# Local Codex recovery state. Durable project state belongs in STATUS/open
# briefs; the Docker build exclusion is a separate `.dockerignore` contract.
/.codex/
