[package]
name = "synveda-gateway-test-support-enabler"
version = "0.0.0"
edition = "2024"
publish = false
license = "Apache-2.0"

[dependencies]
synveda-gateway = { path = "..", features = ["test-support"] }
