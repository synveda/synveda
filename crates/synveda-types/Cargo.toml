[package]
name = "synveda-types"
description = "Domain types, identifiers, and error taxonomy — the root of the crate graph"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
blake3 = { workspace = true }
chrono = { workspace = true }
serde = { workspace = true }
# A dependency rather than a dev-dependency since CPR-3 (ADR-0070): a generic
# scope carries an open `attributes` bag, and what a deployment means by a
# scope is the deployment's to say. A fixed schema there would be the rank
# vocabulary returning under another name.
serde_json = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }
