//! The `synveda` admin/dev CLI (`synveda init`, `synveda policy apply`,
//! `synveda proposal review`, ...). Talks to the gateway API as a client.
//!
//! Implementation lands in Phase 1+ (first commands with FND-2's `synveda init`).

use synveda_types as _;

fn main() {}
