# @synveda — Rust SDK

Placeholder. Thin typed client for the three primitives (`inject`, `recall`,
`observe`) lands with ADPT-4 (Phase 4). SDKs depend only on the public API,
never on core crates (seed §8).
