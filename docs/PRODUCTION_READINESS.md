# Production readiness

Assessment date: 2026-08-27. Scope: the current context-platform checkout as a
service for individuals and small teams. Passing repository gates establishes
repeatable behaviour; it does not establish availability, recoverability,
supportability or a releasable supply chain.

**Verdict: Not ready.** Three P0 readiness gaps remain: the release does not
publish the chart and CNPG-compatible image the Helm shape names; the new
existing-Secret KMS wiring has no production-shaped restart/restore evidence or
owned custody and rotation procedure; and there is no backup/PITR and restore
drill. P1 gaps also leave the gateway single-replica, restart-shaped, without
production abuse controls, operational SLOs or a tested tenant lifecycle. A
controlled evaluation remains useful, but it is not a production deployment.

CPR-45 and ADR-0102 now define a Docker-first reference deployment programme.
Its acceptance can prove controlled single-host installation, Keycloak OIDC,
separate workers, private telemetry and local isolated recovery. It cannot by
itself close the three production P0s: release parity still needs pullable
published artifacts, key custody still needs an owner-approved production
ceremony, and backup/restore still needs off-host retention plus recurring
measured drills against owned RPO/RTO.

The status vocabulary below is `Ready`, `Conditional`, `Not ready` and
`Not assessed`. Severity is the consequence of leaving the gap standing, not a
score for the implemented code.

## Docker reference programme

| Scope | Current status | Exit evidence | Explicit residual limit |
|---|---|---|---|
| Development Compose | Not ready | Canonical render plus clean-volume HTTP login/product lifecycle on a supported desktop Docker platform | Development HTTP and loopback/operator exposure are not reference security evidence. |
| Single-host reference | Not ready | Clean Linux and desktop runs of proxy-only HTTPS, Keycloak, gateway/worker, Collector, governed product lifecycle, backup/PITR, isolated database+key restore, restart and tested-version upgrade | One host/server, planned maintenance and same-host-backup risk; no HA or DR claim. |
| External dependency shape | Not assessed | Same image, deterministic configuration tests and live conformance for each provider actually named | Schema/configuration evidence alone is not provider support. |
| Hosted playground promotion | Not ready | Exact pullable artifact manifest, off-host recovery, custody, basic abuse/lifecycle controls, alerts and runbooks in addition to reference acceptance | May remain single-host only when host-loss and maintenance limits are explicit. |
| Private-beta SaaS promotion | Not ready | All P0s plus multi-replica correctness, durable worker reliability, lifecycle/isolation, load/soak, supportability and supply-chain evidence | Docker reference evidence is necessary but not sufficient. |

## Runtime and availability

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Gateway availability, statelessness and multi-AZ | Not ready | P1 | Helm fixes `replicas: 1` and `Recreate` (`deploy/helm/synveda/templates/gateway.yaml:1-16`). Pending login/handoff state is process-local (`crates/synveda-identity/src/flow.rs:217-231`); the chart refuses replica settings (`deploy/helm/synveda/templates/_helpers.tpl:101-105`). | `make chart-lint`; `make check-deploy`; live kind acceptance deletes the CloudNativePG primary and repeats the same public context run after promotion | No cross-pod login, cache-invalidation bound, rolling upgrade or gateway failover. Database failover does not make the request plane HA. | A three-replica test begins login on one pod, finishes on another, observes a scope mutation on every pod within a stated bound, and loses a pod during traffic without an incorrect decision. | OPS-7: durable login/handoff rows, cross-process policy/entity invalidation, explicit worker ownership, then lift the chart refusal. | Choose invalidation transport and leader mechanism; define availability target and acceptable staleness. |
| Startup, liveness, readiness, drain and shutdown | Not ready | P1 | `/healthz` is process-only and `/readyz` checks Postgres plus schema epoch (`crates/synveda-gateway/src/app.rs:214-243`). SIGINT and SIGTERM both enter Axum graceful request shutdown, after which background tasks are aborted (`crates/synveda-gateway/src/main.rs:431-486`). | Exact SIGTERM subprocess regression; Helm liveness/readiness probes (`deploy/helm/synveda/templates/gateway.yaml:144-169`); `make smoke` | Request shutdown is signal-correct, but there is no readiness withdrawal, worker checkpoint wait, bounded worker join or demonstrated termination-grace sequence. | A process test sends SIGTERM during requests and claimed work, observes readiness fail first, completes or safely releases work, flushes telemetry, and exits within the pod grace period. | Add a cancellation token shared by server/workers, readiness-draining state and bounded joins. | Set pod termination grace period and drain SLO. |
| Durable jobs, retry, backpressure and dead-letter handling | Not ready | P1 | Capture claims durable batches with a lease, but the worker retries a failed sweep on each poll (`crates/synveda-ingest/src/capture_worker.rs:65-109`) and may perform one model call per frozen event (`crates/synveda-ingest/src/capture_worker.rs:228-260`). Work is embedded in the gateway process. | Capture/store DB tests; capture counters and extractor latency (`crates/synveda-ingest/src/capture_worker.rs:42-47`) | No durable next-attempt schedule, exponential backoff/jitter, terminal dead-letter state, queue-age SLO or global provider concurrency quota. Restart safety exists at the batch lease; operational overload control does not. | Repeated provider failure creates bounded attempts with persisted next-attempt time, a terminal inspectable state, queue-age metrics and no retry storm; restart and two-worker tests prove one effect. | Extend the existing capture-batch lease/state machine; add a bounded semaphore and operator retry/dead-letter commands rather than a second queue product. | Define provider quotas, maximum retry age and manual-retry policy. |
| Provider degradation and cache correctness | Conditional | P1 | Context embedding has a timeout and lexical fallback (`crates/synveda-gateway/src/context_api.rs:1058-1091`); graph work has governed hop/fan-out/candidate/time/token ceilings (`crates/synveda-types/src/configuration.rs:211-275`). Scope/entity invalidation is process-local (`crates/synveda-gateway/src/app.rs:73-81`). | Context-run tests; planner-stage and degradation telemetry; configuration tests | Read-path degradation is explicit, but provider circuit breaking and cross-process invalidation are absent. There is no context-result cache, so no production cache hit-rate or invalidation evidence exists. | Dependency outage preserves authorised lexical results with a stable degradation code; a multi-replica mutation cannot serve stale authority; provider calls remain within configured concurrency and timeout bounds. | Keep lexical fallback; add dependency health/circuit metrics and solve invalidation as part of OPS-7 before adding any result cache. | Choose acceptable provider-degradation window and whether semantic-only tenants may accept lexical fallback. |

## Authentication and tenancy

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Browser, CLI and service-token lifecycle | Conditional | P1 | Console sessions are sealed in Postgres with a 12-hour cap (`crates/synveda-gateway/src/auth.rs:327-405`); CLI refresh exists (`crates/synveda-gateway/src/auth.rs:92-114`); service tokens require a bounded `iat`/`exp` lifetime (`crates/synveda-gateway/src/authz.rs:216-220,622-639`). | OIDC, console-session, CLI-login and service-identity tests | No general access-token revocation list, device binding or revocation-within-bound guarantee; refresh-token rotation depends on the IdP. Synveda has scoped service identities and provisioning credentials, not a general-purpose API-key product. | Revoked user/service credentials fail every API within 30 seconds; rotated refresh tokens cannot replay; session inventory/revoke is audited; service identity disable remains immediate. | AUTH-6: persist revocations by issuer/token identifier, add session inventory/revoke and exercise rotation/replay. | IdP must expose usable revocation/rotation claims; owner decides device-bound requirements. |
| Federated identity uniqueness | Not ready | P1 | JIT login resolves by `(tenant_id, subject)` (`crates/synveda-gateway/src/provision.rs:130-155`; `crates/synveda-store/src/identities.rs:99-114`), and the schema enforces that pair without issuer (`crates/synveda-store/migrations/0001_context_platform.sql:5142-5146`). Multiple issuers are configurable. | Single-issuer OIDC/JIT tests; MEM-7 remains open | Two trusted issuers may issue the same `sub`, collapsing distinct people onto one principal scope, grants and private Knowledge. No admission constraint proves one issuer per tenant. | Either identity keys include canonical issuer plus subject end to end, or tenant admission rejects a second issuer; collision, relink, revoke and directory-projection tests prove no authority transfer. | MEM-7: define the issuer-qualified stable identity key and a hard-cut/update path, or enforce and test the narrower one-issuer product contract. | Identity owner chooses multi-issuer support, link proof/recovery and existing-identity treatment. |
| Rate limits, quotas and abuse controls | Not ready | P1 | The request router applies tenant resolution and per-handler PDP checks (`crates/synveda-gateway/src/app.rs:164-180`), but no general rate-limit layer or quota store appears in that assembly. SCIM maps a pre-existing `RateLimited` error to 429 (`crates/synveda-gateway/src/scim/mod.rs:140-158`), but its credential verifier contains no limiter (`crates/synveda-gateway/src/scim/mod.rs:232-293`). | HTTP request counters/latency; domain input bounds | No per-tenant/principal/IP request limits, ingest/token/storage quotas, concurrency budgets or abuse alarms. Input caps and a response taxonomy do not bound request frequency or tenant cost. | Adversarial load proves independent tenant/principal limits, fair queuing and bounded DB/provider work; responses use stable 429 envelopes and auditable quota changes without denied-resource count leakage. | Add a small policy-neutral limiter at the authenticated boundary plus durable tenant quota/configuration counters; start with session-event bytes/rate and context concurrency. | Product owner must set plans/quotas and support override rules; deployment chooses distributed limiter storage. |
| RLS, PDP and cross-tenant isolation | Conditional | P1 | Tenant tables enable and force RLS and requests set tenant context before Cedar decisions (`docs/SECURITY.md:9-22`). The adversarial inventory pins forced-RLS, identifier-oracle, principal privacy, context side-channel and VedaFlow evidence (`scripts/check-context-security.mjs:13-40`). | `make check-context-security`; `crates/synveda-store/tests/rls.rs`; `crates/synveda-gateway/tests/foundation_audit.rs`; deterministic and 10k security gates | Strong current evidence covers named boundaries, but TEN-6's continuous API fuzzing remains open; DB superuser, host and compromised gateway are outside the boundary (`docs/SECURITY.md:115-124`). | Every tenant table is forced-RLS; direct-SQL and public-API generated probes cover all authenticated operations nightly with zero cross-tenant disclosures; changed routes enter the inventory automatically. | Extend the current route catalogue/security generator rather than creating a parallel harness; add restore-shaped and owner-role cases. | Security owner must define supported adversary and frequency/retention of nightly evidence. |
| Tenant provisioning, suspension, export and erasure | Not ready | P1 | Storage explicitly supports admit and resolve only (`crates/synveda-store/src/tenants.rs:1-5`); suspended tenants fail resolution (`crates/synveda-gateway/src/tenant.rs:265-289`). Sealed export has no re-import or tenant-erasure claim (`docs/INSTALL.md:789-798`). | Tenant-resolution tests; sealed-export tests; Knowledge-level forget tests | No complete public tenant suspend/resume administration, portable re-import, ordered populated-tenant erasure or destruction certificate. Deployment reset is not tenant erasure. | A populated tenant can be suspended, exported, restored into a fresh instance, erased with zero tenant references, and given a verifiable content-free destruction certificate; interruption resumes safely. | Re-cut TEN-5 against epoch 3: inventory all tenant FKs, durable ordered lifecycle state machine, restore format/version check and erasure verification. | Legal/privacy owner defines retention, holds and certificate semantics. |
| Directory integrations and support access | Not ready | P1 | SCIM and bounded Entra/Okta pull implementations exist, but live Entra/Okta evidence is explicitly absent (`docs/SECURITY.md:130-135`). Break-glass/local authority is documented as an operator exception (`docs/SECURITY.md:9-17`). | Directory fixtures and gateway/store tests; content-free audit chain | No live vendor tenant verification, provider-degradation SLO, audited time-bound support-access workflow or tested break-glass recovery/rotation ceremony. | Each claimed provider passes create/update/group/remove/disable/rate-limit recovery live; support access is requested, approved, expiring and chained; break-glass drill closes and rotates credentials. | Keep SCIM and pull on one projection; add provider-specific live suites and a narrowly scoped support grant workflow. | Requires real vendor tenants/credentials and an owner-approved support/break-glass policy. |

## Data durability

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Backups, PITR, restore drills, RPO and RTO | Not ready | P0 | The Helm values deliberately contain no backup stanza (`deploy/helm/synveda/values.yaml:75-94`); OPS-5 requires a PITR playbook and tested RPO/RTO (`docs/backlog/OPS-5.md:13-19`). | None for production backup/restore; database tests create disposable databases, not recovery evidence | No scheduled backup, object-store retention, WAL archive, PITR, restore validation, RPO/RTO or recurring drill. One storage failure can exceed any unstated recovery objective. | Encrypted backups and WAL archive restore a production-shaped cluster to a chosen point; current schema, Knowledge heads/index rebuild and audit chain verify; measured RPO/RTO meet documented targets in a recurring drill. | OPS-5: CNPG backup/object-store configuration, restore job, post-restore verifier and monthly isolated drill. | Owner selects region/object store, retention, RPO/RTO, key custody and cost. |
| Schema migration, upgrade and rollback | Not ready | P1 | The current pre-1.0 epoch refuses old databases instead of migrating them (`docs/INSTALL.md:815-834`); reinstalling is the current upgrade path (`docs/INSTALL.md:800-813`). Helm uses `Recreate` (`deploy/helm/synveda/templates/gateway.yaml:14-16`). | Epoch/hard-cut checks; migration tests; `make check-deploy` | No supported data-preserving old-epoch upgrade, release compatibility window, rollback procedure or zero-downtime database/application ordering. | An N-1 production-shaped backup upgrades to N, validates contract/data/audit, and either rolls application/schema back within a declared window or restores by a measured procedure; incompatible builds refuse before serving. | Define the first post-1.0 compatibility policy, expand migration fixtures, and add an upgrade/rollback CI environment. | Product owner decides pre-1.0 reset policy end date and supported release window. |
| Index reconstruction, re-embedding and plan stability | Not ready | P1 | Embeddings retain model and dimension and graph/search can fall back; current dense-query planning can switch after prepared-statement reuse, documented in CTX-7 (`docs/backlog/CTX-7.md:3-13`). | `make eval-retrieval`; ANN/latency benchmarks; Knowledge index metrics | No production rebuild runbook/SLO or generation cutover evidence; dense plan choice is not stable; 1M/5M/20M filtered scale is not established. | Rebuild from restored Postgres creates a named generation, serves old generation until atomic cutover, reports lag, and meets measured recall/latency at representative tenant sizes; execution 1 and 6 use the chosen plan. | Close CTX-7 first, then add resumable generation rebuild/cutover and restore drill coverage. | Choose supported embedding models/dimensions and maximum tenant corpus. |
| Audit restoration, WORM, SIEM and legal hold | Not ready | P1 | The chain is tamper-evident and content-minimised, not WORM (`docs/SECURITY.md:30-32,115-120`). AUD-3 and AUD-4 remain scheduled signed export and SIEM work (`docs/backlog/AUD-3.md:13-19`; `docs/backlog/AUD-4.md:13-19`). | Audit append/verify metrics; API/export verification tests | A DB superuser can delete the chain; no external anchor/object lock, continuous SIEM stream, restored-chain drill or tenant/legal-hold extension. | Frozen-head export verifies offline from object-locked storage; restored DB verifies the same prefix; SIEM delivery is retryable/idempotent and monitored; holds prevent governed erasure where policy requires. | Build one durable audit export/outbox feeding object lock and OTLP/CEF, reusing canonical hash inputs. | Compliance owner chooses WORM target, retention, hold and SIEM schema. |
| Data export, retention and erasure coverage | Conditional | P1 | Knowledge forget removes authorised plaintext/embeddings/source payloads and retains content-free evidence (`docs/SECURITY.md:100-113`); the sealed tenant export includes Knowledge history and audit but is memory-resident and has no import (`docs/INSTALL.md:789-798`). | Knowledge lifecycle/erasure tests; sealed-export tests | Erasure is aggregate-specific, legal hold is incomplete, full tenant restoration is absent, and large exports are not streamed with a production size/duration envelope. | Property tests inventory every content-bearing table; erasure/hold outcomes are complete and retryable; export streams within byte/time bounds and imports into a clean tenant with identical canonical evidence. | Complete TEN-5 around a versioned streaming archive; keep Knowledge forget as the lower-level primitive. | Privacy owner defines subject/tenant scope and required evidence retention. |

## Security and supply chain

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Dependency, npm, container and CI supply chain | Conditional | P1 | `cargo-deny` enforces sources, advisories and licence policy (`deny.toml:4-70`); CI also checks npm/corpus/container inventories (`.github/workflows/ci.yml:45-111`). Images/actions are tag-major or tag-version references rather than immutable digest/SHA pins (`.github/workflows/ci.yml:17-25`; `deploy/helm/synveda/values.yaml:121-129`). | `cargo deny check`; `make check-npm-licences`; `make check-chart-images` | Inventory is not vulnerability scanning or provenance. No container/npm advisory gate, image digest lock, CI-action SHA pinning or documented remediation SLA. | Every released dependency/image/action has an immutable identity, vulnerability result and approved exception with expiry; release blocks critical findings and emits the inventory used. | Add lockfile/container scanners and action/image pin checks to release CI; keep current licence gates. | Security owner defines severity SLA and exception authority. |
| Inbound transport and Kubernetes workload isolation | Not ready | P1 | Helm accepts an `http://` public URL (`deploy/helm/synveda/templates/_helpers.tpl:108-125`), ingress TLS is optional (`deploy/helm/synveda/values.yaml:66-74`), and the gateway ServiceAccount token is mounted by Kubernetes default (`deploy/helm/synveda/values.yaml:192-205`; `deploy/helm/synveda/templates/gateway.yaml:30-37`). The chart has no NetworkPolicy. | Helm render/lint and kind functional acceptance | No production profile proves encrypted external transport, least-privilege pod identity or bounded ingress/egress. A compromised gateway pod has avoidable Kubernetes credentials and unrestricted network reach. | A production render requires HTTPS or an explicitly tested trusted TLS terminator, disables unused token automount, applies default-deny NetworkPolicies with named dependencies, and passes login/context while plaintext ingress and undeclared egress fail. | Add a production values contract, ServiceAccount automount control and explicit ingress/egress policies; extend kind acceptance with negative probes. | Operator chooses ingress/TLS/DNS/egress endpoints, certificate ownership and any genuine Kubernetes API need. |
| Release signing, SBOM and provenance | Not ready | P1 | Release produces SHA256SUMS but states that artifacts are unsigned (`.github/workflows/release.yml:298-306,375-377`). No SBOM, signature or attestation step is present in the release workflow. | Release workflow dry run; checksum verification in installer | Checksums detect transfer corruption but do not authenticate the builder; no SBOM, SLSA-style provenance, notarisation or revocation process. | Each archive/image/chart has an SBOM, identity-bound signature and verifiable build attestation; installer verifies before replacing files; compromised-key revocation is documented and tested. | Add CycloneDX/SPDX generation, keyless or managed signing for artifacts/images, provenance attestations and macOS notarisation. | Legal/security owner must choose publisher identity, signing custody and notarisation accounts. |
| Key plane, rotation and secret custody | Not ready | P0 | The gateway fails closed without a usable local key (`crates/synveda-gateway/src/main.rs:489-512`). Helm requires an externally owned Secret and injects both the key and stable reference (`deploy/helm/synveda/templates/gateway.yaml:96-110`); its executable contract check rejects missing wiring without rendering plaintext. A live kind install now completes OIDC sign-in and governed API work through that wiring. Local tenant-key rotation/re-encryption exists (`docs/INSTALL.md:770-798`). | Store/CLI key tests; `make chart-lint`; live kind login/context acceptance; key-open/rotation metrics | Startup and sign-in are proven, but no production-shaped run proves secret-value rotation, restart and joint database/KEK restore. There is no cloud KMS/HSM/customer-managed provider, automated KEK rotation or owned custody ceremony. | A fresh Helm install signs in, stores/rotates/opens a tenant secret, restarts and restores from backup without plaintext key in values/rendered output; KEK loss and wrong-key cases fail visibly. | Extend the kind acceptance through key use, rotation, restart and restore; then implement a separately reviewed custody provider and ceremony selected by the owner. | Owner must choose custody provider, Secret delivery, backup and rotation policy. |
| SSRF, archive traversal and bounded external input | Conditional | P2 | Request bodies inherit a 2 MiB ceiling and riskier DTO/import readers add tighter bounds (`docs/SECURITY.md:95-98`). OKF remote import rejects private addresses and redirects (`docs/SECURITY.md:125-131`); directory clients have 5s connect/30s request timeouts (`crates/synveda-identity/src/directory/mod.rs:385-400`). | `make check-context-security`; OKF, directory and archive tests | Tenant export/open still assembles or reads whole archives in memory; egress allowlists/network policy and decompression/streaming resource telemetry are not production-proven. | Boundary tests cover redirects, DNS rebinding, symlinks, file count, expanded bytes, slow bodies and cancellation; archive/export memory stays bounded under the maximum supported tenant. | Stream the local archive format with a hard byte limit; deploy egress NetworkPolicies and retain the existing URL validation. | Deployment owner defines allowed egress and maximum archive/tenant size. |
| MCP, Tool, Skill and prompt-poisoning boundary | Conditional | P1 | Gateway Tool tests are discovery-only; it never launches imported stdio commands or proxies arbitrary tool calls (`docs/SECURITY.md:23-29,125-129`). Skill validation executes no bundle code; Configuration narrows external providers (`crates/synveda-types/src/configuration.rs:410-426`). | Tool/Skill/OKF tests; `make check-context-security`; security evaluation | No MCP OAuth execution proxy, enterprise egress policy, artifact signature/revocation feed or live poisoning red-team evidence. Local client stdio runs in the user's trust domain. | Revoked Skill/Tool versions disappear at the next resolution; signed provenance and exact version are visible; remote discovery/OAuth obeys scope/egress/timeout budgets; poisoning corpus stays at zero leakage/unauthorised invocation. | Keep discovery and invocation separate; add signature/revocation metadata first, then design OAuth execution only with a concrete client need. | Security/product owner decides whether Synveda ever proxies execution and which signature authorities are trusted. |
| Vulnerability and incident response | Not ready | P1 | `docs/SECURITY.md` is an implementation boundary and adversarial inventory, not a vulnerability-reporting policy or incident runbook (`docs/SECURITY.md:1-7,137-154`). | Security gates and audit chain | No published reporting channel/SLA, severity/triage process, credential-compromise playbook, customer notification process or practiced incident exercise. | A report has a private intake, owner, SLA and disclosure policy; a tabletop rotates signing/KEK/provider credentials, preserves evidence and produces customer-safe communication. | Add root `SECURITY.md` policy plus concise incident/credential-compromise runbooks linked to release and operations evidence. | Legal/security owner supplies contact, disclosure SLA and notification obligations. |

## Observability and supportability

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Traces, metrics, logs and sensitive-content defaults | Conditional | P1 | One telemetry module installs OTel/Prometheus and describes HTTP, PDP, capture, audit, key and planner signals (`crates/synveda-gateway/src/telemetry.rs:1-32,310-324,410-435`). Security tests pin content-minimised audit and adapter redaction (`docs/SECURITY.md:66-93`). | `/metrics`; OTel exporter; observability and redaction tests | No production collector/retention profile, log schema/version contract, end-to-end dropped-telemetry alarm or documented sensitive-field allowlist for support export. | A production-shaped outage/error can be traced gateway-to-store/provider without payloads; telemetry loss is alerted; a fixture scan proves secrets/session/Knowledge bodies absent from default exports. | Ship collector/dashboard examples and a machine-checked telemetry field policy; add release markers. | Owner chooses telemetry backend, region and retention. |
| SLOs, alerts, usage, quota and cost | Not ready | P1 | Request latency and context-token histograms exist (`crates/synveda-gateway/src/telemetry.rs:18-32,300-324`); eval reports p50/p95 and tokens (`crates/synveda-eval/src/report.rs:590-610`). | Eval baselines; raw Prometheus metrics | No availability/error-budget SLOs, alert thresholds, queue-age/index-lag/provider-health alerts, per-tenant usage/quota model or cardinality-reviewed cost dashboard. | Published SLOs have recording/alert rules and synthetic checks; per-tenant usage is aggregatable without tenant/principal labels on hot metrics; alerts link to runbooks and release markers. | Define a small SLI set: availability, context latency/error, ingest lag, capture retry age, index lag and restore age; emit bounded internal tenant accounting separately. | Product/operations owners set SLOs, budget semantics and on-call. |
| Customer and internal operations UX | Not ready | P1 | The console exposes product views for Sessions, Context, Knowledge, Skills and Tools, but there is no current customer operations overview or internal SaaS operator plane. Existing `/metrics` is an infrastructure endpoint, not a tenant-safe monitoring product. | Console tests; domain APIs; Prometheus | Customers cannot distinguish live/failed sessions, context efficiency, Knowledge health, registry drift or quota state in one place; operators lack tenant/region, queue, DB/index, backup, release and incident views. | Customer views disclose only authorised aggregates and show loading/partial/stale/degraded states; internal views show regional/version/queue/DB/provider/backup health with audited support access. | First expose bounded aggregate health APIs and shared status primitives; build customer Overview before any internal support console. | Decide SaaS region model, cost source and support-access authority. |
| Runbooks, release markers and support workflow | Not ready | P1 | Install and security limits are documented, but backup/restore, rollback, incident, provider-outage and audited support workflows remain absent or open. | None as an exercised operations suite | No owned runbook index, drill cadence, release marker in telemetry or support escalation workflow. | Every P0/P1 alert links to a tested runbook; deploy emits version/schema/config markers; quarterly exercises record restore, provider outage, key loss and rollback evidence. | Add a concise operations index after the corresponding mechanisms exist; exercise through disposable environments. | Assign service owner, on-call, support tiers and drill cadence. |

## Performance and cost

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Load, soak and concurrency | Not ready | P1 | There are focused ingest/concurrency tests and deterministic evals, but no production-shaped multi-hour soak or mixed-tenant gateway/provider load gate. Helm is one gateway with a 20-connection default pool (`deploy/helm/synveda/values.yaml:25-55`). | `crates/synveda-gateway/tests/session_ingest_load.rs`; DB concurrency tests; benchmarks | No maximum supported request/event/context concurrency, pool-saturation behaviour, memory/disk slope, noisy-neighbour result or leak-free soak evidence. | A declared small-team workload runs for 24 hours with bounded memory/disk/queue age, no cross-tenant fairness failure, stable p95/p99 and recovery after DB/provider faults. | Build one mixed workload from public APIs, then tune explicit gateway/provider/DB concurrency bounds before horizontal scale. | Define supported team size, session/event rate, retention and test hardware. |
| Context/ingest/extraction latency, query plans and growth | Not ready | P1 | Planner-stage and extractor histograms exist (`crates/synveda-gateway/src/context_api.rs:76-83,1918-2038`; `crates/synveda-ingest/src/capture_worker.rs:42-47`). Dense plan stability is unresolved (`docs/backlog/CTX-7.md:3-13`). | Deterministic/retrieval evals; ANN benchmarks; Postgres tests | No production p99 context target by corpus size, event-ingest/extraction throughput SLO, pool sizing study, index-lag objective, query-plan regression gate for critical SQL or storage-growth model. | Representative 1x/10x corpora publish p50/p95/p99, throughput, pool wait, plans, index lag and byte growth; regression thresholds fail without baseline rewriting. | Stabilise dense plans, add pool-wait/index-lag metrics and benchmark the top query families with `EXPLAIN (ANALYZE, BUFFERS)`. | Set supported corpus/event retention and latency/cost budget. |
| Token budgets, disclosure and measured value | Conditional | P1 | Governed context limits requests to 100,000 tokens and graph work to smaller explicit ceilings (`crates/synveda-types/src/configuration.rs:21-36,173-200`). The default profiles use 1,500 context tokens and bounded graph budgets (`crates/synveda-types/src/configuration.rs:511-547`). Deterministic and BGE-M3 baselines gate token/quality metrics (`evals/baseline.json:3-27`; `evals/baseline-retrieval.json:3-36`). | `make eval`; `make eval-retrieval`; tokens-per-context histogram | Budgets are Configuration/profile values, not an evidenced harness/model matrix. There is no billing-grade provider cost, cost per useful context run, prompt-cache hit evidence or workload-level outcome/latency/cost trade study. Subscription harness token cost cannot be inferred as billed spend. | Reports join exact delivered tokens, latency, helpfulness/correction and provider-reported usage when available; every supported harness/model has a measured bounded budget; estimates are labelled; quality cannot regress to meet a token target. | Add a versioned outcome/cost report around existing context feedback and eval signals before changing selection. | Harness/provider must expose reliable token/cache/billing data; owner defines “useful.” |

## Release and platform

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Release/Helm artifact parity and reproducibility | Not ready | P0 | The release publishes `ghcr.io/synveda/gateway` and a non-CNPG `ghcr.io/synveda/postgres` image (`.github/workflows/release.yml:261-281,338-349`). Helm instead defaults to `synveda/gateway` and the CNPG-compatible but unpublished `synveda/enterprise-postgres:17` (`deploy/helm/synveda/values.yaml:16-20,81-83`), and the release does not package/publish the chart. The source Dockerfiles now build fail-closed, and a live kind run preloaded both images, completed OIDC login, workspace/session/context/audit operations, deleted the database primary and repeated the context run after promotion (`demos/ops-2-helm-install.sh`). | `make chart-lint`; `make check-chart-images`; executable Helm/image-input checks; live source-image kind acceptance | Source build and installation are proven, but a customer still cannot pull the named product/CNPG image pair and chart from one release; chart and application version have no published atomic unit. | A tagged release publishes chart plus both pullable multi-arch images from the exact Dockerfiles it references; install in an empty registry-authenticated cluster completes login/context; every accepted chart enum boots. | Make the release workflow the one artifact authority, package/sign the chart, publish the CNPG image it actually references, and run the existing live acceptance from an empty authenticated registry. | Choose OCI chart/image registry and retention; release owner supplies publish credentials. |
| Installer, upgrade, rollback and deployment parity | Not ready | P1 | Host install has checksum and executable smoke coverage, but upgrades reinstall/restart (`docs/INSTALL.md:800-813`). Helm is restart-shaped and the database hard cut has no translator. | Release-install demo; `make check-deploy`; chart lint | No supported rollback, N-1 compatibility test, zero-downtime upgrade, package manager or production Compose/Helm parity against the same published artifacts. | N-1 to N and failed-N rollback run in installed-host and Helm shapes with preserved data/key; versions/schema/contract are checked before traffic; previous artifact remains recoverable. | Add versioned release manifest, compatibility matrix and upgrade/rollback acceptance after fixing artifact parity and DR. | Owner sets support window and acceptable upgrade outage. |
| Platforms, version support and project licence | Not ready | P1 | Release builds macOS arm64 and Linux x86_64 and declares unsigned limits (`.github/workflows/release.yml:78-139,375-377`). There is no repository `LICENSE`, while OpenAPI metadata names `Proprietary` (`crates/synveda-gateway/src/openapi.rs:86`); README records the unresolved mismatch (`README.md:105-107`). | Native binary smoke in release workflow | Windows/Linux arm64 binaries and version windows are absent. A metadata label without complete owner-approved terms does not establish external distribution/use rights. | A support matrix names tested OS/arch/Postgres/Kubernetes versions and lifecycle; CI exercises each claimed target; owner-approved `LICENSE`/notices ship in every artifact and generated metadata agrees. | Add targets only after native CI/install evidence; reconcile the legal metadata and release contents through an owner decision. Do not invent a licence. | Legal/owner decision is required for the first-party licence and support policy. |
| SaaS deployment and residency | Not ready | P1 | Current Helm is one regional installation and OPS-3 residency routing remains open (`docs/backlog/OPS-3.md:13-19`). No control-plane/data-plane regional routing or SaaS tenant operations are present. | Single-cluster deployment checks | No regional isolation, tenant placement/move, multi-region failover, billing, regional support boundary or SaaS provisioning/suspension/erasure control plane. | A tenant pinned to a region keeps data/embeddings within it under network-policy tests; failover and support access preserve that boundary; lifecycle and DR meet regional RPO/RTO. | Treat as a later control-plane feature; do not add speculative abstractions to the single-region core. | Hosting topology, jurisdictions, subprocessors and billing are owner decisions. |

## Harness support

`adapters/registry.json` is the executable support authority. It distinguishes a
configuration recipe, captured frames, deterministic replay and a live verified
lifecycle (`adapters/registry.json:2-20`). Only Claude Code is one of the four
harnesses below and appears in that registry; a Codex Skill output directory is
not lifecycle support (`crates/synveda-cli/src/skill.rs:20-23`).

| Harness | Configured | Authentic frames captured | Deterministic replay | Live verified | Lifecycle events | Durable spool | Context delivery | Skill support | MCP support | Compaction/checkpoint support | Current tested version | Limitations |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Claude Code | Yes, plugin | Yes, digest-pinned manifest | Yes | Yes, recorded 2026-08-24 | SessionStart, UserPromptSubmit, Pre/PostToolUse, PreCompact, Stop, SessionEnd | Yes, atomic local event spool | Yes, at SessionStart/resume | Sync/advertise; activation is not observable | Yes, plugin-owned launch | PreCompact durably records before rewrite; reinjection only when SessionStart fires with compact source | 2.1.241 live; 2.1.220 also tested | Host death before a hook is invisible; Stop/PreCompact cross only local durability; same-account attacker can rewrite/re-hash spool; skill activation is not claimed (`adapters/registry.json:24-65`). |
| Codex | No lifecycle integration in registry | No | No | No | None established | No | No | Filesystem materialisation target only; no activation/lifecycle evidence | No registered conformance | None established | None | The `.codex/skills` target is a distribution convenience, not captured frames, context delivery or support. |
| GitHub Copilot | No | No | No | No | None established | No | No | No conformance evidence | No registered conformance | None established | None | Absent from the authoritative registry; do not infer support from generic MCP/Skills compatibility. |
| Pi | No | No | No | No | None established | No | No | No conformance evidence | No registered conformance | None established | None | Absent from the authoritative registry; no contract, fixture, replay or live run is committed. |

**Harness status: Not ready (P1) beyond the one verified Claude Code
lifecycle.** Adding a row requires a named client version, authentic frames,
deterministic replay and live public-API evidence for every applicable criterion
in `adapters/registry.json:10-20`; generated MCP configuration alone is not
verification.

## Context optimisation

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Current retrieval, provenance, freshness and graph bounds | Conditional | P1 | Context uses a bounded candidate universe and excludes transitional heads (`crates/synveda-gateway/src/context_api.rs:85-95,1007-1014`), narrows the governed token budget (`crates/synveda-gateway/src/context_api.rs:879-885`), and deduplicates item ids/content before exact PDP and stale/superseded filtering (`crates/synveda-gateway/src/context_api.rs:1312-1394,1471-1497`). Graph work has two-hop/fan-out/time/token ceilings (`crates/synveda-types/src/configuration.rs:211-275`). | Context-run/security tests; planner-stage metrics; deterministic/BGE-M3 evals | Exact provenance and bounds are strong, but production corpus scale, correction outcomes, dense-plan stability and extractive passage selection are incomplete. Current selection budgets whole Knowledge revisions rather than a measured passage-before-summary stage. | At representative scale, current/non-transitional authorised revisions are deterministic for the same frozen evidence; denied candidates affect no output/trace; passage selection improves or preserves quality under a lower token budget with exact source offsets. | Stabilise the dense plan and add a pure, revision-addressed passage selector before any summariser. | Choose supported corpus/model/tokenizer and passage-quality threshold. |
| Stable prefix and provider prompt-cache alignment | Not ready | P1 | No context-result or provider prompt-cache implementation appears in the runtime. Context blocks are freshly composed and content-hashed per run (`crates/synveda-gateway/src/context_api.rs:2010-2023`). | Tokens-per-context and eval token metrics only | No deterministic prefix contract, cache-control integration, cache hit/eviction telemetry or measured provider savings. Caching before authorisation would be unsafe. | A byte-stable authorised prefix is separated from volatile task/recent content; provider cache evidence is measured; any local cache key includes tenant, principal-permission digest, project, Configuration version, Knowledge head, index generation, model and harness; PDP filtering precedes insertion. | Specify/render the stable prefix as a pure tested function, then trial provider cache controls behind one measured adapter. Do not add a shared result cache first. | Provider/harness must expose cache semantics and trustworthy usage data. |
| Task state, recent window, checkpoints and context offload | Not ready | P1 | Sessions retain a task summary and ordered events; Claude derives a start query from the current transcript (`adapters/claude-code/src/session-start.mts:101-119`). PreCompact records transcript state to the spool but does not create a structured summary/checkpoint (`adapters/claude-code/src/turn.mts:6-25`). CTX-6 remains open (`docs/backlog/CTX-6.md:13-19`). | Claude replay/live lifecycle; session timeline tests | No typed task/checkpoint artifact, governed recent-working-window policy, probe-tested compression, or fetch/pin/offload tool contract. Replaying full event text is not a checkpoint. | Pre-compaction writes a versioned, provenance-bound checkpoint; restart retrieves task state + bounded recent window + long-term Knowledge without duplication; probe eval proves key facts/corrections survive; fetch/pin tools re-authorise every access. | Implement CTX-6 as a session artifact/candidate feeding the existing capture/VedaFlow seam; design offload tools only after a live harness need. | Harness must expose a reliable compact/restart boundary; owner chooses checkpoint retention and sensitivity. |
| Progressive Skill/Tool disclosure | Conditional | P1 | Configuration independently narrows Skill/Tool advertisement (`crates/synveda-types/src/configuration.rs:342-350`); exact Skill versions and Tool bindings are separately governed. Claude syncs Skills on SessionStart (`adapters/claude-code/hooks/hooks.json:3-20`). | Skill/Tool gateway tests; Claude skill tests; context tests | No cross-harness measured metadata/body/on-demand disclosure policy, Tool body fetch protocol or activation outcome. Advertising every visible item has no production token/cost study. | Metadata tier is bounded and exact-versioned; bodies/files/tools are fetched only on demand with fresh PDP; disclosure reduces tokens without reducing task success; unsupported clients make no claim. | Add disclosure metrics and one Claude experiment using current exact version APIs before changing shared contracts. | Harness capabilities and activation callbacks are external constraints. |
| Model routing, passage selection and outcome/cost measurement | Not ready | P1 | Capture supports deterministic, Claude and vLLM extractors and Configuration allowlists provider families (`crates/synveda-gateway/src/main.rs:481-523`; `crates/synveda-types/src/configuration.rs:410-426`). Eval distinguishes deterministic, BGE-M3 and credentialed model tiers. | `make eval`; `make eval-retrieval`; optional live extraction/LongMemEval targets | No production routing policy by task/risk/cost, extractive-before-summary pipeline, billing-grade prompt-cache savings or cost-per-useful-run dashboard. Model extraction remains credential-dependent, and subscription-client spend is not observable. | Routing decisions name exact model/configuration and stay within provider allowlist; passage-before-summary preserves provenance; labelled evaluations report helpfulness, correction, latency and actual provider usage where available; estimates are explicit. | Add offline routing experiments to the evaluation harness first; promote only a measured closed vocabulary through governed Configuration. | Requires model credentials/endpoints, provider usage fields and product-defined quality/cost thresholds. |

## P0/P1 gap register

These entries are implementation slices, not substitute feature IDs. Before
work begins, each maps to the named existing feature or one new catalogue/
backlog/STATUS entry. The evidence and current gap are the corresponding area
rows above.

| ID / feature | Severity | Exact scope and architecture seam | Non-goals | Acceptance tests | Rollout and rollback | Dependencies / owner decision |
|---|---|---|---|---|---|---|
| PR-01 / release artifact parity | P0 | Make the tagged release workflow publish the exact chart, gateway image and CNPG-compatible image named by Helm. | No product-profile branch or chart redesign. | Pull all artifacts from an empty authenticated registry and complete login plus one context run; verify signatures/digests and existing chart contract checks. | Publish a candidate tag first; retain the previous atomic manifest and chart version for rollback. | OCI registry, release credentials, publisher identity and retention. |
| PR-02 / key custody acceptance | P0 | Exercise the existing Helm Secret/local-KMS seam through key use, rotation, restart and joint DB/key restore. | No cloud KMS/HSM implementation until its provider/custody decision is accepted. | Correct key restores and opens data; missing, stale and wrong keys fail closed; rendered artifacts/logs contain no plaintext. | Rehearse on isolated restored data; keep the prior KEK/envelopes until verification completes and document reversal. | Security owner chooses custody, delivery, backup and rotation ceremony. |
| PR-03 / OPS-5 | P0 | Add encrypted CNPG base backup/WAL archive, isolated PITR restore and post-restore schema/Knowledge/audit/index verification. | No multi-region failover or invented RPO/RTO. | Restore to chosen points before/after writes, verify frozen audit prefix and rebuilt indexes, and measure recurring RPO/RTO. | Enable on a non-production cluster, validate restore, then stage retention; rollback disables new scheduling without deleting valid backups. | Object store/region, retention, key custody, RPO/RTO and drill owner. |
| PR-04 / OPS-7 + drain | P1 | Persist login handoff, provide cross-process PDP/entity invalidation, withdraw readiness on termination and join/release workers within a bound. | No distributed cache product or worker-service split without evidence. | Three replicas finish cross-pod login, see an authority mutation within the bound, survive pod loss, and drain in-flight requests/leases on SIGTERM. | Shadow invalidation lag first; retain one-replica/Recreate settings until the complete test passes, with rollback to that shape. | Availability/staleness/drain SLO and invalidation/leadership choice. |
| PR-05 / capture reliability | P1 | Extend the existing capture-batch lease with persisted next-attempt/terminal state, jittered bounded retry and provider concurrency limits. | No Temporal or second queue plane. | Repeated failures do not storm; two workers/restarts produce one effect; terminal work is inspectable/retryable; queue age is bounded and alerted. | Deploy retry state before enabling new scheduling; rollback readers must tolerate the added state and safely retain leases. | Provider quotas, retry age/count and manual retry policy. |
| PR-06 / AUTH-6 | P1 | Add session inventory/revoke and issuer/token-identifier revocation at the existing credential verification seam. | No proprietary IdP or device-binding design without owner requirements. | Revocation takes effect within the declared bound, refresh replay fails, disable remains immediate and every action is audited. | Start with observe-only revocation telemetry, then enforce; rollback preserves records but can disable the new check only during incident recovery. | IdP claims/revocation behaviour and security-owner bound. |
| PR-07 / rate limits and quotas | P1 | Bound authenticated request frequency/concurrency and durable tenant usage at the gateway boundary without hot metric identity labels. | No billing/plans product or denied-resource counts. | Fair tenant/principal isolation under adversarial load, stable 429 envelopes, bounded DB/provider work and audited quota mutation. | Shadow-count before enforcement; enable per operation/tenant class and retain an audited emergency override. | Product quota semantics and distributed limiter storage. |
| PR-08 / TEN-5 | P1 | Build a restart-safe tenant lifecycle over the epoch-3 FK/table inventory: suspend/resume, streaming export/import and verified erasure. | Deployment reset is not tenant erasure; no legal-hold semantics without policy. | A populated tenant suspends, restores into clean storage and erases to zero tenant references with content-free certificate; interruption resumes. | Dry-run inventory first; hold source data until restore verification, and make destructive execution force-explicit/non-reversible. | Privacy/legal retention, hold and certificate decisions. |
| PR-09 / AUD-3 + AUD-4 | P1 | Feed canonical frozen-head audit export through a durable idempotent outbox to object lock and a documented SIEM schema. | No historical Cedar replay or content-bearing audit payload. | Object-locked prefix verifies offline and after DB restore; duplicate/retry delivery is harmless; lag/failure alerts fire. | Dual-write to non-authoritative sink first; rollback stops delivery without mutating the canonical chain. | WORM/SIEM targets, retention and compliance owner. |
| PR-10 / operational signals | P1 | Define bounded SLIs/alerts/runbooks and tenant-safe aggregate health APIs for session, context, Knowledge, registry, provider, backup and release state. | No full internal SaaS console or high-cardinality tenant labels on hot metrics. | Synthetic failures page with runbook/release marker; customer views distinguish loading/partial/stale/degraded without denied counts. | Ship recording rules/dashboards dark, tune from evidence, then alert; rollback alerts without removing raw signals. | Telemetry backend, on-call, regions, retention and SLOs. |
| PR-11 / signed supply chain | P1 | Add SBOM, identity-bound signing/provenance, immutable action/image references and vulnerability policy to release CI. | No hand-rolled crypto or silent baseline acceptance. | Installer verifies artifacts; every chart/image/archive has matching SBOM/attestation; critical findings and expired exceptions fail release. | Sign candidate artifacts before enforcing verification; retain last trusted manifest and documented key revocation. | Publisher identity, signing custody/notarisation and vulnerability SLA. |
| PR-12 / OPS-6 | P1 | Define N-1 compatibility and exercise data/key-preserving installed-host and Helm upgrade/rollback ordering. | No compatibility shim for deleted pre-1.0 epochs. | N-1 upgrades to N, failed N restores/rolls back within the window, and incompatible binaries refuse before traffic. | Canary restored data first; keep previous application artifact and measured restore path until the window closes. | Supported release window, outage budget and first post-1.0 migration policy. |
| PR-13 / licence and platform policy | P1 | Reconcile OpenAPI `Proprietary` metadata with owner-approved repository/release terms and publish the tested OS/arch/Postgres/Kubernetes window. | The agent does not choose licence terms or claim untested platforms. | Release contents/metadata agree; every claimed platform has native build/install evidence and lifecycle dates. | Do not distribute as supported until decided; metadata-only rollback is not a substitute for legal review. | Legal owner, support owner and CI platforms. |
| PR-14 / CTX-7 + EVAL-6 | P1 | Stabilise dense query planning, publish the supported corpus/concurrency envelope and measure context/event/capture/pool/index/storage behaviour. | No benchmark-baseline rewrite to accept regressions. | Representative mixed tenants report p50/p95/p99, throughput, pool wait, plans, lag, growth and soak failures against reviewed thresholds. | Run as non-gating evidence, set thresholds from reviewed runs, then gate; rollback the code regression rather than the floor. | Hardware/model/corpus envelope and performance budget. |
| PR-15 / outcome and cost evidence | P1 | Join exact context feedback, delivered tokens/latency and provider-reported usage where available into the evaluation report. | Subscription estimates are not billing; quality may not be traded away silently. | Workloads report helpfulness/correction and actual or explicitly estimated cost per useful run; provenance and PDP filtering remain exact. | Evaluate offline first; change governed selection only after a measured result and revert by binding the prior Configuration version. | Provider usage fields and product definition of useful outcome. |
| PR-16 / CPR-39 | P1 | Apply registry conformance to one additional real harness from authentic frames through replay and live lifecycle. | MCP configuration or inspected vendor docs alone are not support. | Every required criterion has digest-pinned evidence and a named live version; outage/retry/context limits are explicit. | Land as captured first, promote to verified only after live evidence, and demote automatically when a required criterion drifts. | Proprietary client executable, credential and stable lifecycle contract. |
| PR-17 / CTX-6 + cache alignment | P1 | Add a versioned provenance-bound checkpoint and measured stable-prefix experiment at one real harness boundary. | No shared result cache, summary without evidence, or cache before PDP. | Compaction/restart preserves facts/corrections under a bounded window; any cache key includes all authority/version dimensions named above and measured quality does not regress. | Trial behind one governed Configuration version; rollback binds the prior version and retains checkpoint evidence. | Harness compact/restart hook, provider cache semantics and retention/sensitivity decision. |
| PR-18 / MEM-7 | P1 | Make federated identity uniqueness issuer-qualified through login, directory projection, principal scope and grant resolution, or enforce one issuer per tenant at admission. | No heuristic email linking or silent merge of existing principals. | Two issuers with the same `sub` cannot share scope/grants/Knowledge; link/unlink, revoke, relogin and directory tests retain exact authority and audit evidence. | Hard-cut before external production data, or deploy the one-issuer admission refusal first; rollback must never merge principals. | Multi-issuer product decision, issuer canonicalisation and owner-approved recovery/link proof. |
| PR-19 / Kubernetes boundary | P1 | Require a production TLS termination contract, disable unused ServiceAccount token automount and add default-deny ingress/egress NetworkPolicies around the existing Helm workload. | No service mesh mandate or invented Kubernetes API dependency. | Production render and kind probes complete login/context through declared dependencies while HTTP/plaintext, Kubernetes credential access and undeclared egress are refused. | Add as an opt-in hardened profile, verify dependency allowlists, then make it the supported production shape; rollback restores the prior chart without claiming production readiness. | Ingress/certificate owner, DNS/IdP/TEI/OTLP/directory endpoint inventory and cluster CNI support. |

P0 closure is necessary but not sufficient for a changed verdict. Reassess the
P1 register after the release, restore and key-plane acceptance suites run
together against the same production-shaped artifact set.
