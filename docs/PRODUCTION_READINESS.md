# Production readiness

Assessment date: 2026-09-24. Scope: the current context-platform checkout as a
self-hosted service. Passing repository gates establishes
repeatable behaviour; it does not establish availability, recoverability,
supportability or a releasable supply chain.

The 2026-09-19 [Kubernetes deployment audit](../deploy/README.md#kubernetes-release-contract)
defines OPS-11's narrower next release. The portable chart increment implements
external PostgreSQL/OIDC, verified TLS/private CAs, file-mounted Secrets and
bounded migration/tenant convergence; current measured checks are recorded in
[OPS-11](backlog/OPS-11.md). ADR-0110 adds the persistent starter with a locked
optional Keycloak chart, retained CNPG Cluster, explicit owner/member admission
and scoped service credentials. Platform and release qualification remain
separate and do not close this register's custody or recovery gaps.

On 2026-09-19 its independent PostgreSQL/Keycloak Kind fixture passed private-CA
TLS (database client certificates included), real PKCE and Session/context/audit,
negative credential/CA/hostname/issuer/audience checks, pod restarts and a
same-source Helm migration rerun. The application endpoint was private HTTP.
Public HTTPS ingress, cloud providers, OpenShift, CNPG failover on this increment,
joint restore and published installation remain unverified.

The subsequent ADR-0110 starter run passed all four PostgreSQL/Keycloak ownership
combinations through a private-CA HTTPS fixture proxy. It covered explicit owner,
member/viewer/stranger and cross-workspace denial, existing service/MCP access,
400 captured events and 80 context runs per case, planned pod recreation,
same-source upgrade, retained reinstall and unchanged-token revocation. The
[resource report](../demos/evidence/ops11-starter.json) records short-run server
CPU/memory peaks; test clients, operator and cluster overhead are excluded.
This adds single-instance continuity evidence, not ingress-controller,
OpenShift, managed-cloud, registry-pull, soak, N-1 upgrade or joint-restore
qualification. Initial resource requests are not capacity guarantees.

ADR-0111 adds explicit restricted workload settings, assigned-ID handling, edge
Routes with organisation certificate references, opt-in NetworkPolicies and a
restricted-v3 post-renderer in the existing chart. Structural checks cover the
OpenShift 4.19/4.20 API targets; no real OpenShift or managed-cloud environment
was available. The [portability guide](../deploy/helm/synveda/PORTABILITY.md)
separates local restricted-UID evidence from outstanding SCC, router, CNI/CSI,
dependency and provider qualification. This does not change the verdict below.

The 2026-09-20 ADR-0112 operational increment adds locally packaged-chart
acceptance for claimed Capture interruption, concurrent migration, repeated
public-API workloads and native logical restore into fresh storage. Restore
checks preserve issuer/subjects, membership, Knowledge retrieval, a durable
Session payload, the original sealed console session and a frozen audit prefix.
The drill found and fixed a migration-stamp race by retaining SQLx's existing
advisory lock through the complete migration boundary. Exact results and
durations are in [OPS-11](backlog/OPS-11.md) and the
[starter](../demos/evidence/ops11-operations-cnpg-packaged.json) and
[external](../demos/evidence/ops11-operations-external-external.json) reports. This local drill
does not qualify encrypted off-host custody, WAL/PITR, a published N-1 upgrade,
production RPO/RTO or recurring drills.

**Verdict: Not ready.** Published v0.4.3 establishes native Linux AMD64/ARM64
candidate deployment/recovery, anonymous public image/chart checks and an
attested 35-asset inventory. Its tagged workflow passed all qualification but
failed after draft upload on a GitHub draft-by-tag lookup; the exact verified
draft was promoted by ID. The earlier v0.4.1 and v0.4.2 runs stopped before
their final releases. Published installation evidence does not establish HA or
production readiness.
Release parity also remains conditional on the optional CNPG deployment path; production key
custody/rotation and backup/PITR with recurring live restore remain P0 gaps.
P1 gaps also leave the gateway single-replica, restart-shaped, without production
abuse controls, operational SLOs or a tested tenant lifecycle. A controlled
evaluation remains useful, but it is not a production deployment.

The 2026-09-20 [ADR-0115](adr/adr-0115-prebuilt-container-release-verification.md)
increment prepares required release gates that anonymously pull the complete
image set on fresh native AMD64/ARM64 runners, check source/version/platform,
then exercise the packaged Docker lifecycle and four Kubernetes ownership modes
before creating the GitHub Release. The
[server-only archive guide](../deploy/compose/PREBUILT.md) removes source-build,
host Node/OpenSSL and hostname-edit prerequisites from loopback evaluation.

The published [v0.4.0 release](https://github.com/synveda/synveda/releases/tag/v0.4.0)
passed anonymous image pulls, real browser/CLI authentication, optional repeatable
sample Capture, preserved credentials/data across recreation, deliberate fault
refusal and paired logical restore on native Linux AMD64/ARM64. Both runners
also passed the packaged chart's four ownership modes, restricted admission,
assigned UID, migration refusal/recovery, retained reinstall and joint restore.
The [installation record](backlog/CPR-45.md#installation-mission-2026-09-20)
links the six checksummed reports and exact source/image identities. All 15
public assets were anonymously downloaded and matched the qualified bytes;
the OCI/downloadable chart archives are identical. The upload-only workflow
failure was recovered using its original artifacts, without rebuilding or
moving the tag. Local macOS/OrbStack candidate evidence remains separately labelled.

The [v0.4.1 tagged run](https://github.com/synveda/synveda/actions/runs/35900269117)
passed exact-source native OCI and six client archive candidate jobs, copied
the images to Docker Hub and GHCR, and assembled release assets. Both native
anonymous verification jobs then failed. The linked ARM64 job reported its
first Docker Hub image mismatch because the verifier expected `docker.io/` in
Docker's familiar `RepoDigests` entry.
Compose/Helm verification, final attestation and GitHub Release publication
were skipped. The immutable tag and registry coordinates remain preserved;
the public v0.4.3 release passed its separate native and anonymous gates.
This does not qualify Docker Desktop/WSL2, real OpenShift, a supported N-1
upgrade or production custody.

The [v0.4.3 tagged run](https://github.com/synveda/synveda/actions/runs/36062182772)
passed both native Compose and Helm candidate drills, copied the six-image set
to Docker Hub and GHCR, and passed anonymous public image/chart checks on both
architectures. It attested and uploaded all 35 assets, then failed at a
post-upload draft-by-tag lookup. The exact draft was checked against the signer,
source SHA, expected asset set and every server-computed digest before promotion.
The [public release](https://github.com/synveda/synveda/releases/tag/v0.4.3)
and `SHA256SUMS` are anonymously retrievable. The [CI guide](CI.md) records the
exact-source main and dry-run gates and the failed workflow/recovery boundary.

Docker Desktop, Windows/WSL2, real OpenShift, an optional CNPG controller-backed
deployment of these published bytes, and a supported published N-1 pair remain
unqualified. CPR-45's single-host and Kind drills do not establish production
custody/rotation, encrypted off-host retention, PITR or recurring measured
recovery against owned RPO/RTO.

The 2026-09-21 OPS-12 plain-Compose candidate separately passed its 20-check
local artifact lifecycle and named-volume recovery gate on macOS arm64 with
OrbStack. It quiesces the source, restores the logical database pair into a
private empty target, verifies the original sealed keys and audit chain, and
reuses the original browser identity/sample receipt. An initial empty-directory
copy defect was fixed and the complete drill rerun from fresh extracted bytes.
The candidate used cached local images plus a rebuilt PostgreSQL image; native
Linux anonymous release qualification was still pending at that checkpoint.
Release CI now requires
separate consumer reports on both architectures. This adds local recovery
evidence, not encrypted off-host custody, PITR or a changed readiness verdict.

ADR-0116 adds native consumer lifecycle, public-API project setup
and managed Claude/registry-backed MCP registration. Private receipts bind
deployment ownership and exact adapter entries; managed observation requires
matching project/profile consent. The source macOS arm64 CLI started the earlier
extracted candidate on OrbStack, passed real browser login/API/logout and shut
down with all volumes retained. This local run alone did not qualify the
subsequently published native clients. User-wide hook consent, Codex/Copilot
automatic hook setup and a supported upgrade window remain open.

The v0.4.1 OPS-12 candidate packages the existing CLI and three adapters with
private checksum-pinned Node, retaining its complete licence notices. The
client installer downloads no server artifacts and uses private ownership and
content receipts with an atomic release selection. Native build jobs passed
separate archive and installed-binary reports for four Unix and two Windows
targets; no public v0.4.1 client archives exist. Real issuer/harness use, OS
signing/notarization and broader platform support remain unqualified. This does
not change the readiness verdict or the published v0.4.0 installation contract.

ADR-0117 isolates the Unix deployment peer witness and shares client path
fixtures between Rust and Node. Missing HOME no longer selects a repository-local
spool. The ADR-0117 candidate extends private storage to Rust receipts and
version-1 spools plus Windows Node hooks through a bounded local CLI protocol.
Snapshot comparison refuses stale writes/removals; unsafe or oversized files
remain held. On source `418669f`, both native architectures passed strict Clippy,
Rust storage and mock-gateway refresh checks, and all five Rust/Node private-state
checks. [OPS-12](backlog/OPS-12.md) records the exact hosted job identities.
Real issuer login and harness use remain unqualified.

ADR-0065 amendment 13 adds a Windows ZIP/PowerShell installation candidate with
protected staging, bounded archive/PE checks and native private-file operations.
Windows uses an atomic `current.json` selection and a digest-checked PowerShell
launcher; it retains prior releases and user state. Native Windows x64 and arm64
passed all seven archive checks on clean source `418669f`, including PowerShell
install/reinstall, restricted-PATH execution, storage interoperability and
archive/installer refusals. The downloaded x64 archive hashes and native identities
match its report. Exact source, native job and arm64 evidence-bundle identities,
plus x64 local-file timings, are in [OPS-12](backlog/OPS-12.md).
Windows deployment, setup/vendor writers and diagnostic logs still refuse.

The status vocabulary below is `Ready`, `Conditional`, `Not ready` and
`Not assessed`. Severity is the consequence of leaving the gap standing, not a
score for the implemented code.

## Docker reference programme

| Scope | Status | Exit evidence | Limit |
|---|---|---|---|
| Bundled loopback release | Conditional | Published v0.4.0 passed anonymous retrieval, installation, authentication, sample, recreation and paired restore on native Linux AMD64/ARM64; local macOS/OrbStack candidate also passed | v0.4.1 publication failed; localhost HTTP, one host, Docker Desktop/WSL2, off-host custody and N-1 remain unqualified |
| Development Compose | Not ready | Repeat the clean-volume browser/product lifecycle on Linux and Docker Desktop; one macOS/OrbStack run passed | Explicit loopback HTTP; no reference security claim |
| Single-host reference | Not ready | Linux and desktop HTTPS runs with the restart matrix, joint logical database/key restore, Apalis canary and the implemented Operations view | One host; planned interruption; no HA, owned RPO/RTO or DR |
| External OIDC | Not assessed | Same product image boots against a named conformant provider | Configuration-only evidence is not provider support |
| Hosted promotion | Not ready | Published artifacts, off-host recovery, custody, abuse controls and runbooks | Compose evidence is necessary, not sufficient |

The current canonical graph has a proxy-only public edge, separate Synveda and
Keycloak databases/roles, production-mode Keycloak, containerized gateway and
worker, mounted secret files and a private OpenTelemetry Collector. Static
Compose checks cover the provider/runtime/OTLP matrix plus optional
observability and Apalis renders. The direct acceptance command, fixed restart
matrix and two-principal public-API team scenario are implemented and
deterministically tested. On 2026-09-10, the clean development path also passed
on macOS 26.6.2 arm64 with OrbStack Docker Engine 29.4.0 and Compose 5.1.2,
including PKCE browser login, worker Capture completion, exact issuer recovery
after PostgreSQL/Keycloak restarts and persisted receipt verification. A
subsequent canonical down/up retained the exact PostgreSQL volume and generated
key set. That one host run does not prove reference TLS, Linux or Docker Desktop
support, live database recovery or a published installation.

ADR-0105 removed the clean-engine/Colima simulation because it supplied no live
Docker evidence. Direct Compose/browser and native restart acceptance still
await repetition on Linux and Docker Desktop; the Compose logical recovery
entry points remain live-unproved on that supported-host matrix. The separate
Kind drill above does not close it. The logical database pair,
separate KMS/Keycloak recovery set and fresh private restore are implemented
and deterministically tested. The same-schema product-image upgrade lifecycle
and its read-only compatibility refusal are also implemented and
deterministically tested, but await live reference evidence. The
disabled-by-default Apalis 0.7.4 `skill_validation@1` canary is implemented
behind forced-RLS operation/attempt/outbox state with the native worker as
rollback. Its separate queue holds only opaque routing references, not business
state. Fresh isolated exact-role PostgreSQL acceptance covers its authoritative
operation/outbox concurrency, retry, cancellation, dead-letter and tenant
isolation; live Apalis startup/delivery/restart evidence remains pending. The
customer-safe Operations route is implemented from four bounded public APIs;
it deliberately labels the operational signals those APIs cannot yet provide.
Executable external PostgreSQL plus external OIDC and external OTLP wiring are
implemented deterministically but remain live-validation pending.

Deterministic logical recovery checks are implementation evidence only and do
not close the production P0. Live recovery on a supported host, encrypted
off-host retention, WAL/PITR, RPO/RTO and recurring drills remain open.
OpenTelemetry traces establish the core seam. External mode forwards traces
over public-PKI TLS through the same private Collector with bounded in-memory
queueing; private CA, authentication, mTLS and remote-receipt evidence remain
open. The optional bounded local
Collector-to-Prometheus path is implemented with loopback-only operator access,
72-hour/1-GB TSDB block-retention thresholds (not a disk quota) and
deterministic lifecycle/smoke checks. Live metrics visibility and richer
tenant-safe operational aggregates remain open; a general dashboard/support
platform remains later supportability work.

## Runtime and availability

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Gateway availability, statelessness and multi-AZ | Not ready | P1 | Helm fixes `replicas: 1` and `Recreate` (`deploy/helm/synveda/templates/gateway.yaml:1-16`). Pending login/handoff state is process-local (`crates/synveda-identity/src/flow.rs:217-231`); the chart refuses replica settings (`deploy/helm/synveda/templates/_helpers.tpl:101-105`). | `make chart-lint`; `make check-deploy`; current live Kind acceptance deleted the CloudNativePG primary, observed promotion, repeated the same public context run and rechecked worker readiness | No cross-pod login, cache-invalidation bound, rolling upgrade or gateway failover. Database failover does not make the request plane HA. | A three-replica test begins login on one pod, finishes on another, observes a scope mutation on every pod within a stated bound, and loses a pod during traffic without an incorrect decision. | OPS-7: durable login/handoff rows, cross-process policy/entity invalidation, explicit worker ownership, then lift the chart refusal. | Choose invalidation transport and leader mechanism; define availability target and acceptable staleness. |
| Startup, liveness, readiness, drain and shutdown | Not ready | P1 | The gateway retains process liveness, database/schema readiness and graceful HTTP signal handling. The separate core worker defaults to loopback-only health/readiness/metrics; the optional Compose observability profile permits only an explicit unspecified-address bind on private networks and publishes no worker port. It refuses work until schema, exact runtime role and initial policy convergence pass, continually re-proves epoch/role through a supervised authority sentinel, withdraws readiness on SIGINT/SIGTERM, cancels and joins supervised tasks within a configured 3–300 second bound, closes its pool and flushes telemetry (`crates/synveda-gateway/src/worker.rs`; `crates/synveda-gateway/src/shutdown.rs`). Gateway shutdown accepts 2–300 seconds so its cooperative cleanup and forced-join windows are both non-zero. | Real worker subprocess tests cover boot outage, readiness, any-schema owner/elevated/membership/wrong-role refusal, live `BYPASSRLS` drift causing a non-zero critical-task exit and bounded idle SIGTERM; synthetic tests cover drain deadline and configuration bounds. Both Compose manifests give the default 75-second join an 85-second outer window; Helm derives its outer grace as the configured join plus ten seconds. | Gateway shutdown closes its authority gate through the supervised stop signal. The local OPS-11 drill observes cooperative gateway/worker exit and one claimed Capture retry after lease expiry. In-flight HTTP drain, multi-replica shutdown and per-loop progress remain unproved; supervisor heartbeat proves scheduling only. | Process tests send SIGTERM during requests and claimed work, observe readiness fail first, complete or safely release work, flush telemetry and exit within the pod grace period; every permanent authority refusal exits non-zero. | Extend the measured single-instance shutdown through in-flight HTTP and per-loop progress before claiming broader availability. | Set request/worker drain SLOs and the allowed treatment of interrupted provider calls. |
| Durable jobs, retry, backpressure and dead-letter handling | Not ready | P1 | Capture uses the incremented attempt as a tenant/batch/owner fence, re-proves the claim after preflight, renews independently during extractor calls, checks statement-time expiry before terminal transitions, writes no candidate before the completion fence and terminalises an expired final attempt with content-free audit (`crates/synveda-store/src/capture.rs`; `crates/synveda-ingest/src/capture_worker.rs`). Capture, Knowledge indexing, relaxation expiry and optional directory pull run in the separate supervised core worker. The isolated `skill_validation@1` canary separately has persisted next-attempt/cancellation/dead-letter state, attempt/effect fences, queue-age metrics and a transactional outbox; optional Apalis is only its replaceable transport. | Deterministic DB acceptance covers Capture fencing and exhausted-attempt audit plus Skill-validation duplicate dispatch, cancellation, retry/dead-letter and stale-delivery recovery; worker process tests prove strict boot/readiness and bounded idle SIGTERM. | Capture and the other established job families still lack a general durable next-attempt schedule, exponential backoff/jitter, operator retry state, queue-age SLO and global provider concurrency quota. OPS-11 now proves SIGTERM during claimed Capture with two attempts and one fenced candidate; two-worker execution and live Apalis delivery remain unproved. External provider effects are not exactly once. | Repeated provider failure creates bounded attempts with persisted next-attempt time, an inspectable retry/dead-letter state, queue-age metrics and no retry storm; restart, blocked-provider SIGTERM and two-worker tests prove one effect. | Retain claimed-work interruption coverage, then extend Capture's own retry schedule and bounded provider semaphore; do not generalise the Apalis experiment or migrate existing job families without separate evidence. | Define provider quotas, maximum retry age and manual-retry policy. |
| Provider degradation and cache correctness | Conditional | P1 | Context embedding has a timeout and lexical fallback (`crates/synveda-gateway/src/context_api.rs:1058-1091`); graph work has governed hop/fan-out/candidate/time/token ceilings (`crates/synveda-types/src/configuration.rs:211-275`). Scope/entity invalidation is process-local (`crates/synveda-gateway/src/app.rs:109-116`). | Context-run tests; planner-stage and degradation telemetry; configuration tests | Read-path degradation is explicit, but provider circuit breaking and cross-process invalidation are absent. There is no context-result cache, so no production cache hit-rate or invalidation evidence exists. | Dependency outage preserves authorised lexical results with a stable degradation code; a multi-replica mutation cannot serve stale authority; provider calls remain within configured concurrency and timeout bounds. | Keep lexical fallback; add dependency health/circuit metrics and solve invalidation as part of OPS-7 before adding any result cache. | Choose acceptable provider-degradation window and whether semantic-only tenants may accept lexical fallback. |

## Authentication and tenancy

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Browser, CLI and service-token lifecycle | Conditional | P1 | Console sessions are sealed in Postgres with a 12-hour cap (`crates/synveda-gateway/src/auth.rs:327-405`); CLI refresh exists (`crates/synveda-gateway/src/auth.rs:92-114`); service tokens require a bounded `iat`/`exp` lifetime (`crates/synveda-gateway/src/authz.rs:216-220,622-639`). | OIDC, console-session, CLI-login and service-identity tests | No general access-token revocation list, device binding or revocation-within-bound guarantee; refresh-token rotation depends on the IdP. Synveda has scoped service identities and provisioning credentials, not a general-purpose API-key product. | Revoked user/service credentials fail every API within 30 seconds; rotated refresh tokens cannot replay; session inventory/revoke is audited; service identity disable remains immediate. | AUTH-6: persist revocations by issuer/token identifier, add session inventory/revoke and exercise rotation/replay. | IdP must expose usable revocation/rotation claims; owner decides device-bound requirements. |
| Federated identity uniqueness | Conditional single-issuer; federation not ready | P1 | Persistent lookup still uses `(tenant_id, subject)`. ADR-0110 now rejects overlapping static issuer/tenant bindings and refuses a claim-bound issuer alongside another issuer (`crates/synveda-identity/src/oidc.rs`). | OIDC admission tests reject static/static and claim/static overlap before network or token use; single-issuer JIT and exact-role database suite | No durable issuer-qualified binding or governed provider replacement/linking. Replacing a tenant's issuer in place, directory email adoption and multi-issuer federation remain unsupported; configuration rejection alone does not migrate identities. | Canonical issuer/subject bindings, migration, relink, revoke and directory-projection tests prove no authority transfer across persisted identities. | MEM-7: complete issuer-qualified binding history, proof/recovery and migration while retaining the narrower starter admission boundary. | Identity owner chooses link proof, recovery and treatment of existing identities. |
| Rate limits, quotas and abuse controls | Not ready | P1 | The request router applies tenant resolution and per-handler PDP checks (`crates/synveda-gateway/src/app.rs:199-245`), but no general rate-limit layer or quota store appears in that assembly. SCIM maps a pre-existing `RateLimited` error to 429 (`crates/synveda-gateway/src/scim/mod.rs:140-158`), but its credential verifier contains no limiter (`crates/synveda-gateway/src/scim/mod.rs:232-293`). | HTTP request counters/latency; domain input bounds | No per-tenant/principal/IP request limits, ingest/token/storage quotas, concurrency budgets or abuse alarms. Input caps and a response taxonomy do not bound request frequency or tenant cost. | Adversarial load proves independent tenant/principal limits, fair queuing and bounded DB/provider work; responses use stable 429 envelopes and auditable quota changes without denied-resource count leakage. | Add a small policy-neutral limiter at the authenticated boundary plus durable tenant quota/configuration counters; start with session-event bytes/rate and context concurrency. | Product owner must set plans/quotas and support override rules; deployment chooses distributed limiter storage. |
| RLS, PDP and cross-tenant isolation | Conditional | P1 | Tenant tables enable and force RLS and requests set tenant context before Cedar decisions (`docs/SECURITY.md:11-31`). The adversarial inventory pins forced-RLS, identifier-oracle, principal privacy, context side-channel and VedaFlow evidence (`scripts/check-context-security.mjs:13-40`). | `make check-context-security`; `crates/synveda-store/tests/rls.rs`; `crates/synveda-gateway/tests/foundation_audit.rs`; deterministic and 10k security gates | Strong current evidence covers named boundaries, but TEN-6's continuous API fuzzing remains open; DB superuser, host, compromised gateway and compromised worker are outside the boundary (`docs/SECURITY.md:247-248`). | Every tenant table is forced-RLS; direct-SQL and public-API generated probes cover all authenticated operations nightly with zero cross-tenant disclosures; changed routes enter the inventory automatically. | Extend the current route catalogue/security generator rather than creating a parallel harness; add restore-shaped and owner-role cases. | Security owner must define supported adversary and frequency/retention of nightly evidence. |
| Tenant provisioning, suspension, export and erasure | Not ready | P1 | Storage explicitly supports admit and resolve only (`crates/synveda-store/src/tenants.rs:1-5`); suspended tenants fail resolution (`crates/synveda-gateway/src/tenant.rs:265-289`). Sealed export has no re-import or tenant-erasure claim (`docs/INSTALL.md`). | Tenant-resolution tests; sealed-export tests; Knowledge-level forget tests | No complete public tenant suspend/resume administration, portable re-import, ordered populated-tenant erasure or destruction certificate. Deployment reset is not tenant erasure. | A populated tenant can be suspended, exported, restored into a fresh instance, erased with zero tenant references, and given a verifiable content-free destruction certificate; interruption resumes safely. | Re-cut TEN-5 against epoch 3: inventory all tenant FKs, durable ordered lifecycle state machine, restore format/version check and erasure verification. | Legal/privacy owner defines retention, holds and certificate semantics. |
| Directory integrations and support access | Not ready | P1 | SCIM and bounded Entra/Okta pull implementations exist, but live Entra/Okta evidence is explicitly absent (`docs/SECURITY.md:263-264`). Bounded bootstrap/operator authority is documented as an exception (`docs/SECURITY.md:20-21`). | Directory fixtures and gateway/store tests; content-free audit chain | No live vendor tenant verification, provider-degradation SLO, audited time-bound support-access workflow or tested break-glass recovery/rotation ceremony. | Each claimed provider passes create/update/group/remove/disable/rate-limit recovery live; support access is requested, approved, expiring and chained; break-glass drill closes and rotates credentials. | Keep SCIM and pull on one projection; add provider-specific live suites and a narrowly scoped support grant workflow. | Requires real vendor tenants/credentials and an owner-approved support/break-glass policy. |

## Data durability

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Backups, PITR, restore drills, RPO and RTO | Not ready | P0 | The bundled Compose path now implements writer-paused PostgreSQL 17 logical archives for both databases, separately linked KMS/Keycloak recovery secrets, fresh private restore, browser login, source-tenant/audit/tenant-key verification and wrong-key refusal (`deploy/compose/scripts/compose.sh`; `deploy/compose/scripts/recovery-set.mjs`). Helm still has no backup stanza, and OPS-5 requires PITR plus owned RPO/RTO. | Deterministic Compose recovery/lifecycle tests; OPS-11 local Kubernetes joint logical restore and public functional verification; no supported-host Compose or production drill | The logical archives are unencrypted, same-host by default, not cross-database atomic and not WAL/PITR. The local Kubernetes drill preserves the quiesced database pair and original key/issuer material, but there is no schedule, object-store retention, owned RPO/RTO or recurring production drill. | Encrypted backups and WAL archive restore a production-shaped cluster to a chosen point; current schema, Knowledge heads/index rebuild and audit chain verify; measured RPO/RTO meet documented targets in a recurring drill. | Run the Compose logical restore on supported hosts for reference validation; separately complete OPS-5 with CNPG/object-store backup, PITR restore and recurring drill. | Owner selects region/object store, retention, RPO/RTO, key custody and cost. |
| Schema migration, upgrade and rollback | Not ready | P1 | The current pre-1.0 epoch refuses old databases instead of migrating them (`docs/INSTALL.md`). A same-schema Compose image-transition/rollback smoke is implemented but has not run live, and reinstalling release artifacts is not deployment-upgrade evidence (`docs/INSTALL.md`). Helm uses `Recreate` (`deploy/helm/synveda/templates/gateway.yaml:14-16`). | Epoch/hard-cut and cancellation tests; two live ordinary migrators serialize DDL and stamping under the existing SQLx lock; same-source Helm reruns; `make check-deploy` | No supported published N-1 pair or live Docker reference upgrade exists. Published v0.4.0 and v0.4.3 both mark epoch 3 / baseline revision 3, but migration `0001` differs (`source_payload_hash`), so the exact-checksum compatibility check refuses that pair. Public v0.2.0 predates epoch 3 and remains refused. The Kubernetes runbook requires backup/quiescence and roll-forward or restore when application rollback is incompatible; release compatibility windows and zero-downtime ordering remain open. | An N-1 production-shaped backup upgrades to N, validates contract/data/audit, and either rolls application/schema back within a declared window or restores by a measured procedure; incompatible builds refuse before serving. | Define the first post-1.0 compatibility policy, expand migration fixtures, and add an upgrade/rollback CI environment. | Product owner decides pre-1.0 reset policy end date and supported release window. |
| Index reconstruction, re-embedding and plan stability | Not ready | P1 | Embeddings retain model and dimension and graph/search can fall back; current dense-query planning can switch after prepared-statement reuse, documented in CTX-7 (`docs/backlog/CTX-7.md:3-13`). | `make eval-retrieval`; ANN/latency benchmarks; Knowledge index metrics | No production rebuild runbook/SLO or generation cutover evidence; dense plan choice is not stable; 1M/5M/20M filtered scale is not established. | Rebuild from restored Postgres creates a named generation, serves old generation until atomic cutover, reports lag, and meets measured recall/latency at representative tenant sizes; execution 1 and 6 use the chosen plan. | Close CTX-7 first, then add resumable generation rebuild/cutover and restore drill coverage. | Choose supported embedding models/dimensions and maximum tenant corpus. |
| Audit restoration, WORM, SIEM and legal hold | Not ready | P1 | The chain is tamper-evident and content-minimised, not WORM (`docs/SECURITY.md:18-19,249-250`). AUD-3 and AUD-4 remain scheduled signed export and SIEM work (`docs/backlog/AUD-3.md:13-19`; `docs/backlog/AUD-4.md:13-19`). | Audit append/verify metrics; API/export verification tests; OPS-11 restored frozen-prefix hash equality | A DB superuser can delete the chain; no external anchor/object lock, continuous SIEM stream, recurring production restore drill or tenant/legal-hold extension. | Frozen-head export verifies offline from object-locked storage; restored DB verifies the same prefix; SIEM delivery is retryable/idempotent and monitored; holds prevent governed erasure where policy requires. | Build one durable audit export/outbox feeding object lock and OTLP/CEF, reusing canonical hash inputs. | Compliance owner chooses WORM target, retention, hold and SIEM schema. |
| Data export, retention and erasure coverage | Conditional | P1 | Knowledge forget removes authorised plaintext/embeddings/source payloads and retains content-free evidence (`docs/SECURITY.md:62-66`); the sealed tenant export includes Knowledge history and audit but is memory-resident and has no import (`docs/INSTALL.md`). | Knowledge lifecycle/erasure tests; sealed-export tests | Erasure is aggregate-specific, legal hold is incomplete, full tenant restoration is absent, and large exports are not streamed with a production size/duration envelope. | Property tests inventory every content-bearing table; erasure/hold outcomes are complete and retryable; export streams within byte/time bounds and imports into a clean tenant with identical canonical evidence. | Complete TEN-5 around a versioned streaming archive; keep Knowledge forget as the lower-level primitive. | Privacy owner defines subject/tenant scope and required evidence retention. |

## Security and supply chain

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Dependency, npm, container and CI supply chain | Conditional | P1 | `cargo-deny` enforces sources, advisories and licence policy (`deny.toml:4-70`); CI also checks npm/corpus/container inventories (`.github/workflows/ci.yml:45-111`). Every external deployment Dockerfile base is now required to use a readable tag plus full SHA-256 digest. CI actions are pinned to immutable commits in `.github/action-pins.json`. PR jobs use hosted runners and read-only repository permissions; checkouts retain no token, and reusable Rust/Kind cache publication is limited to main pushes. Chart/runtime support remains subject to its image inventory. | `cargo deny check`; `make check-npm-licences`; `make check-chart-images` | Pinning is not vulnerability scanning or provenance. No container/npm advisory gate, complete runtime image lock or documented remediation SLA. Fork workflow execution for the contributor-hardening increment still requires hosted evidence. | Every released dependency/image/action has an immutable identity, vulnerability result and approved exception with expiry; release blocks critical findings and emits the inventory used. | Add lockfile/container scanners and action/runtime-image pin checks to release CI; keep current licence and Dockerfile-base gates. | Security owner defines severity SLA and exception authority. |
| Inbound transport and Kubernetes workload isolation | Not ready | P1 | Helm requires HTTPS except for its explicit development relaxation. Enabled HTTPS ingress requires a host-matching TLS Secret or an explicit upstream terminator and forwards only application paths. Gateway, worker, install and optional TEI pods disable ServiceAccount-token automount; the chart has no NetworkPolicy. | Helm render/lint and static workload-contract checks; current live Kind acceptance completed Keycloak login/context with token automount disabled | No production profile proves encrypted external transport or bounded ingress/egress, and workload-token/undeclared-egress refusal lacks live negative evidence. | A production render requires HTTPS or an explicitly tested trusted TLS terminator, disables unused token automount, applies default-deny NetworkPolicies with named dependencies, and passes login/context while plaintext ingress, Kubernetes credential access and undeclared egress fail. | Add a production values contract and explicit ingress/egress policies; extend current Kind acceptance with token and network negative probes. | Operator chooses ingress/TLS/DNS/egress endpoints, certificate ownership and any genuine Kubernetes API need. |
| Release signing, SBOM and provenance | Not ready | P1 | v0.4.3 passed exact-source CI, native candidate gates and both public registries. BuildKit SBOM/provenance reached both registries; its 35-asset checksum inventory has a verified GitHub workflow attestation. The tagged job uploaded a complete draft, failed on draft-by-tag lookup, and the exact asset set was verified and promoted by ID (`docs/CI.md`). | `make check-release-parity`; signed inventory and 35 server-computed asset digests; tagged public verification and recovery; PR #63 publication regression | Installer attestation enforcement, archive/chart SBOMs, native code signing/notarisation and revocation remain absent. The tagged final job remains failed even though the verified release is public. | Each archive/image/chart has an SBOM, identity-bound signature and verifiable build attestation; installer verifies before replacing files; compromised-key revocation is documented and tested. | Merge the draft-by-ID publisher fix, then complete consumer attestation enforcement, archive SBOMs and native signing. | CI Result, registry access and the release environment are configured; separate `v*` creation and immutability rulesets are configured; native signing/notarisation accounts remain separate. |
| Key plane, rotation and secret custody | Not ready | P0 | Gateway and worker build the same fail-closed KMS contract through the shared bounded direct/file configuration loader (`crates/synveda-gateway/src/runtime_config.rs`). Helm mounts distinct migrator, gateway and worker database credentials plus issuer/KMS files; tenant convergence alone receives the install Job's KMS projection for key admission. Its executable contract check rejects missing wiring without rendering plaintext. Local tenant-key rotation/re-encryption exists under the install guide's key-management section. | Store/CLI/config tests; `make chart-lint`; current Kind acceptance; key-open/rotation metrics | The local joint database/KEK restore opens a pre-backup sealed console session, but live secret-value rotation and production custody remain unproved. There is no cloud KMS/HSM/customer-managed provider or automated KEK rotation. | A fresh Helm install signs in, stores/rotates/opens a tenant secret, restarts and restores from backup without plaintext key in values/rendered output; KEK loss and wrong-key cases fail visibly. | Retain the joint-restore check, extend it through rotation/refusal, and implement a separately reviewed custody provider and ceremony selected by the owner. | Owner must choose custody provider, Secret delivery, backup and rotation policy. |
| SSRF, archive traversal and bounded external input | Conditional | P2 | Request bodies inherit a 2 MiB ceiling and riskier DTO/import readers add tighter bounds (`docs/SECURITY.md:51-54`). OKF remote import rejects private addresses and redirects (`docs/SECURITY.md:261-262`); directory clients have 5s connect/30s request timeouts (`crates/synveda-identity/src/directory/mod.rs:385-400`). | `make check-context-security`; OKF, directory and archive tests | Tenant export/open still assembles or reads whole archives in memory; egress allowlists/network policy and decompression/streaming resource telemetry are not production-proven. | Boundary tests cover redirects, DNS rebinding, symlinks, file count, expanded bytes, slow bodies and cancellation; archive/export memory stays bounded under the maximum supported tenant. | Stream the local archive format with a hard byte limit; deploy egress NetworkPolicies and retain the existing URL validation. | Deployment owner defines allowed egress and maximum archive/tenant size. |
| MCP, Tool, Skill and prompt-poisoning boundary | Conditional | P1 | Gateway Tool tests are discovery-only; it never launches imported stdio commands or proxies arbitrary tool calls (`docs/SECURITY.md:25-27,259-262`). Skill validation executes no bundle code; Configuration narrows external providers (`crates/synveda-types/src/configuration.rs:410-426`). | Tool/Skill/OKF tests; `make check-context-security`; security evaluation | No MCP OAuth execution proxy, enterprise egress policy, artifact signature/revocation feed or live poisoning red-team evidence. Local client stdio runs in the user's trust domain. | Revoked Skill/Tool versions disappear at the next resolution; signed provenance and exact version are visible; remote discovery/OAuth obeys scope/egress/timeout budgets; poisoning corpus stays at zero leakage/unauthorised invocation. | Keep discovery and invocation separate; add signature/revocation metadata first, then design OAuth execution only with a concrete client need. | Security/product owner decides whether Synveda ever proxies execution and which signature authorities are trusted. |
| Vulnerability and incident response | Not ready | P1 | `docs/SECURITY.md` is an implementation boundary and adversarial inventory, not a vulnerability-reporting policy or incident runbook (`docs/SECURITY.md:1-6`). | Security gates and audit chain | GitHub private vulnerability reporting is enabled and root `SECURITY.md` documents intake and disclosure. No response SLA, severity/triage process, credential-compromise playbook, customer notification process or practiced incident exercise is established. | A report has a private intake, owner, SLA and disclosure policy; a tabletop rotates signing/KEK/provider credentials, preserves evidence and produces customer-safe communication. | Assign private-intake ownership and define incident/credential-compromise runbooks linked to release and operations evidence. | Legal/security owner supplies contact, disclosure SLA and notification obligations. |

## Observability and supportability

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Traces, metrics, logs and sensitive-content defaults | Conditional | P1 | The telemetry module installs OTLP tracing and Prometheus (`crates/synveda-gateway/src/telemetry.rs:212-280,292-336`) and describes PDP, Capture, audit and key signals (`crates/synveda-gateway/src/telemetry.rs:363-382,412-452,485-506`); planner metrics are emitted by the context API (`crates/synveda-gateway/src/context_api.rs:74-81,1897-2019`). Security tests pin content-minimised audit and adapter redaction (`docs/SECURITY.md:218-243`). Canonical Compose optionally uses a closed private Collector scrape/fan-in and digest-pinned Prometheus with loopback-only access and bounded retention; the same Collector can export traces over public-PKI TLS to one external OTLP/gRPC authority. | `/metrics`; OTel exporter; Compose contract/runtime/lifecycle mutation tests; observability and redaction tests | Live Collector-to-Prometheus and external-OTLP visibility are unproved on a supported Docker host. External export has no private-CA, authentication-header or mTLS path, and readiness does not prove remote receipt. The unauthenticated gateway `/metrics` route shares its main listener; canonical Caddy returns 404 for it, and Helm ingress now omits it. Direct ClusterIP access still reaches that listener. There is no production alerting, log schema/version contract, end-to-end dropped-telemetry alarm or documented sensitive-field allowlist for support export. | A production-shaped outage/error can be traced gateway-to-store/provider without payloads; application metrics leave only through a private Collector; telemetry loss is alerted; a fixture scan proves secrets/session/Knowledge bodies absent from default exports. | Prove both Compose telemetry modes live, test ingress route refusal and add a machine-checked telemetry field policy/release marker. | Owner chooses telemetry backend, region and retention. |
| SLOs, alerts, usage, quota and cost | Not ready | P1 | Request latency and context-token histograms exist (`crates/synveda-gateway/src/telemetry.rs:18-32,292-327`); eval reports p50/p95 and tokens (`crates/synveda-eval/src/report.rs:590-610`). | Eval baselines; raw Prometheus metrics | No availability/error-budget SLOs, alert thresholds, queue-age/index-lag/provider-health alerts, per-tenant usage/quota model or cardinality-reviewed cost dashboard. | Published SLOs have recording/alert rules and synthetic checks; per-tenant usage is aggregatable without tenant/principal labels on hot metrics; alerts link to runbooks and release markers. | Define a small SLI set: availability, context latency/error, ingest lag, capture retry age, index lag and restore age; emit bounded internal tenant accounting separately. | Product/operations owners set SLOs, budget semantics and on-call. |
| Customer and internal operations UX | Not ready | P1 | The console Operations route composes bounded durable-operation, Sessions, context-runs and Capture list APIs for the selected project. It shows independent loading/empty/partial/failure states, explicit staleness and safe progress/degradation/failure vocabulary without rendering content, private identifiers, queue-provider identifiers, separately protected candidate counts or zero-filled context-list aggregates. Existing `/metrics` remains a separate infrastructure endpoint. | Operations console tests; generated public API; Prometheus | The public API does not yet expose dependency/worker heartbeat, context latency/token, Knowledge/index, Skill/MCP, backup or provider-health aggregates. There is no internal SaaS operator plane. | Customer views disclose only authorised aggregates and show loading/partial/stale/degraded states; internal views show regional/version/queue/DB/provider/backup health with audited support access. | Add only the missing bounded aggregate APIs justified by customer decisions; do not infer them from infrastructure probes or policy-filtered empty pages. Build any internal support console separately. | Decide SaaS region model, cost source and support-access authority. |
| Runbooks, release markers and support workflow | Not ready | P1 | The Compose guide now documents logical backup, fresh isolated restore, cleanup and interrupted-lock/writer recovery; install and security limits are current. | Deterministic Compose recovery/lifecycle tests plus the OPS-11 native Kubernetes recovery drill and consolidated installation/configuration/operations guide | The Kubernetes runbook covers health, quiescence, native recovery, upgrade refusal and safe uninstall. Owned production alert/runbook coverage, recurring drill cadence, telemetry release markers and audited support escalation remain absent. | Every P0/P1 alert links to a tested runbook; deploy emits version/schema/config markers; quarterly exercises record restore, provider outage, key loss and rollback evidence. | Run the reference recovery guide on disposable hosts, then add production runbooks only as their mechanisms land. | Assign service owner, on-call, support tiers and drill cadence. |

## Performance and cost

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Load, soak and concurrency | Not ready | P1 | There are focused ingest/concurrency tests and deterministic evals, but no production-shaped multi-hour soak or mixed-tenant gateway/provider load gate. Helm is one gateway with a 20-connection default pool (`deploy/helm/synveda/values.yaml:25-55`). | `crates/synveda-gateway/tests/session_ingest_load.rs`; DB concurrency tests; benchmarks | No maximum supported request/event/context concurrency, pool-saturation behaviour, memory/disk slope, noisy-neighbour result or leak-free soak evidence. | A declared concurrent workload runs for 24 hours with bounded memory/disk/queue age, no cross-tenant fairness failure, stable p95/p99 and recovery after DB/provider faults. | Build one mixed workload from public APIs, then tune explicit gateway/provider/DB concurrency bounds before horizontal scale. | Define supported concurrency, session/event rate, retention and test hardware. |
| Context/ingest/extraction latency, query plans and growth | Not ready | P1 | Planner-stage and extractor histograms exist (`crates/synveda-gateway/src/context_api.rs:74-81,1897-2019`; `crates/synveda-ingest/src/capture_worker.rs:42-47,490`). Dense plan stability is unresolved (`docs/backlog/CTX-7.md:3-13`). | Deterministic/retrieval evals; ANN benchmarks; Postgres tests | No production p99 context target by corpus size, event-ingest/extraction throughput SLO, pool sizing study, index-lag objective, query-plan regression gate for critical SQL or storage-growth model. | Representative 1x/10x corpora publish p50/p95/p99, throughput, pool wait, plans, index lag and byte growth; regression thresholds fail without baseline rewriting. | Stabilise dense plans, add pool-wait/index-lag metrics and benchmark the top query families with `EXPLAIN (ANALYZE, BUFFERS)`. | Set supported corpus/event retention and latency/cost budget. |
| Token budgets, disclosure and measured value | Conditional | P1 | Governed context limits requests to 100,000 tokens and graph work to smaller explicit ceilings (`crates/synveda-types/src/configuration.rs:21-36,173-200`). The default profiles use 1,500 context tokens and bounded graph budgets (`crates/synveda-types/src/configuration.rs:511-547`). Deterministic and BGE-M3 baselines gate token/quality metrics (`evals/baseline.json:3-27`; `evals/baseline-retrieval.json:3-36`). | `make eval`; `make eval-retrieval`; tokens-per-context histogram | Budgets are Configuration/profile values, not an evidenced harness/model matrix. There is no billing-grade provider cost, cost per useful context run, prompt-cache hit evidence or workload-level outcome/latency/cost trade study. Subscription harness token cost cannot be inferred as billed spend. | Reports join exact delivered tokens, latency, helpfulness/correction and provider-reported usage when available; every supported harness/model has a measured bounded budget; estimates are labelled; quality cannot regress to meet a token target. | Add a versioned outcome/cost report around existing context feedback and eval signals before changing selection. | Harness/provider must expose reliable token/cache/billing data; owner defines “useful.” |

## Release and platform

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Release/Helm artifact parity and reproducibility | Not ready | P0 | v0.4.3 is the latest complete public release. It passed full native candidate Compose/Helm deployment, anonymous Docker Hub/GHCR execution and OCI chart byte parity; its exact draft was promoted after the post-upload lookup failure. v0.4.1 and v0.4.2 retain incomplete public release states. | `make check-release-parity`; `make chart-lint`; `make check-chart-images`; v0.4.3 tagged native/public reports and signed asset inventory in OPS-12 | Published-registry full deployment was not repeated; the release binds public digests to fully deployed native candidates. Optional CNPG controller-backed use of published bytes, independent image-build reproducibility and per-artifact signature verification remain unproved. | A tagged release publishes the exact chart/reference/six-image set; empty hosts complete login/context and empty clusters boot every accepted chart mode with matching digests. | Qualify optional CNPG and a fresh public v0.4.3 installation from its signed inventory, then test independent image reproducibility and per-artifact signatures. | Operator/cluster authorisation and registry retention. |
| Installer, upgrade, rollback and deployment parity | Not ready | P1 | Deterministic installer evidence proves required checksums, canonical reference archive identity, a two-version `reference/current` update, idempotent reinstall with mutable state preserved and pre-mutation refusal of the retired profile footprint. Automatic artifact removal is deliberately fail-closed pending an OPS-10 ownership receipt. Canonical Compose has a deterministic same-schema product-image lifecycle: the candidate checks exact epoch/ledger/authority read-only, then gateway and worker alone traverse candidate, starting rollback and final candidate while browser/product evidence is repeated. Helm remains restart-shaped and the database hard cut has no translator. | Release-install demo and installer tests; `make check-deploy`; deterministic `compose-upgrade-smoke` lifecycle tests; chart lint | The installed Compose lifecycle has not run against two published images on a supported reference host. There is no receipt-bound artifact uninstall, supported general N-1 window, schema/provider upgrade or downgrade, zero-downtime upgrade, package manager or production Compose/Helm parity. | N-1 to N and failed-N rollback run in installed-host and Helm shapes with preserved data/key; versions/schema/contract are checked before traffic; previous artifact remains recoverable; default uninstall removes only receipt-owned artifacts and preserves recovery state. | Run the bounded installed Compose image smoke from a published manifest, then add the strict OPS-10 ownership receipt and widen the compatibility matrix only after artifact parity and DR. | Owner sets support window and acceptable upgrade outage. |
| Platforms, version support and project licence | Not ready | P1 | v0.4.3 publishes six native client archives and installed-binary reports for Linux, macOS and Windows x64/ARM64. Public v0.4.0 had only macOS ARM64 and Linux x64 server/CLI archives. Apache-2.0 is in root `LICENSE`, with attribution in `NOTICE`; package metadata agrees under ADR-0106. Client packaging retains Synveda and private Node licence notices. | Native source/authentication tests and installed-archive checks; six-target signed release inventory; evidence and gaps in OPS-12 | OS signing/notarization, broader OS/version support windows, real issuer/vendor loading from published client packages and independent verification of all release notice carriage remain open. | A support matrix names tested OS/arch/Postgres/Kubernetes versions and lifecycle; CI exercises each claimed target; root `LICENSE`/`NOTICE` ship in every first-party artifact and generated metadata agrees. | Retain third-party terms and hosted package execution on all six targets; qualify published archives with real issuer/harness use and define a support policy. | First-party licence is decided; support policy and broader platform qualification remain open. |
| SaaS deployment and residency | Not ready | P1 | Current Helm is one regional installation and OPS-3 residency routing remains open (`docs/backlog/OPS-3.md:13-19`). No control-plane/data-plane regional routing or SaaS tenant operations are present. | Single-cluster deployment checks | No regional isolation, tenant placement/move, multi-region failover, billing, regional support boundary or SaaS provisioning/suspension/erasure control plane. | A tenant pinned to a region keeps data/embeddings within it under network-policy tests; failover and support access preserve that boundary; lifecycle and DR meet regional RPO/RTO. | Treat as a later control-plane feature; do not add speculative abstractions to the single-region core. | Hosting topology, jurisdictions, subprocessors and billing are owner decisions. |

## Harness support

`adapters/registry.json` is the executable support authority. It distinguishes a
configuration recipe, captured frames, deterministic replay and a live verified
lifecycle (`adapters/registry.json:2-20`). Claude Code 2.1.241, Codex CLI 0.152.0
and Copilot CLI 1.0.83 have verified lifecycle evidence for their named setups.
A Skill output directory does not establish lifecycle support
(`crates/synveda-cli/src/skill.rs:20-23`).

| Harness | Configured | Authentic frames captured | Deterministic replay | Live verified | Lifecycle events | Durable spool | Context delivery | Skill support | MCP support | Compaction/checkpoint support | Current tested version | Limitations |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Claude Code | Yes, plugin | Yes, digest-pinned manifest | Yes | Yes, recorded 2026-08-24 | SessionStart, UserPromptSubmit, Pre/PostToolUse, PreCompact, Stop, SessionEnd | Yes, atomic local event spool | Yes, at SessionStart/resume | Sync/advertise; activation is not observable | Yes, plugin-owned launch | PreCompact durably records before rewrite; reinjection only when SessionStart fires with compact source | 2.1.241 live; 2.1.220 also tested | Host death before a hook is invisible; Stop/PreCompact cross only local durability; same-account attacker can rewrite/re-hash spool; skill activation is not claimed (`adapters/registry.json:24-65`). |
| Codex CLI | Native MCP and manual trusted-hook setup; no Synveda TOML writer | Yes, digest-pinned MCP/lifecycle/manual/automatic compaction | Yes, protocol and hook/spool replay | Yes, recorded 2026-09-12 | Eight native event types; explicit task-owner Capture/end | Existing spool; four pending outage events delivered once in the final run | Keycloak context, resumed-task recall and compact reinjection pass | Both Skill roots load; no activation claim | MCP 2025-06-18 and text-result persistence pass | Manual/automatic PreCompact and reinjection pass; no structured checkpoint claim | 0.152.0 / GPT-5.5 low on macOS arm64 | Shared SDK task: 19 unique events, 17 Capture candidates, explicit end, reuse and audit verification through sequence 858. Live public-API revoke/re-authorisation passes. Local archive replay passes on Node 22/Linux arm64 and Node 24/macOS arm64. Native execution from a published installation, non-text results and other client versions/platforms remain unqualified. See `docs/integrations/codex.md`. |
| GitHub Copilot CLI | Native MCP command and manual hooks at the Git root | Clean start/resume hooks, shared-workflow transcript and outcome, digest-pinned; prior failures retained | 31 Copilot tests; all 144 adapter tests pass on host/Docker, zero skips | Yes, source build, recorded 2026-09-19 | Start/resume context, agentStop local recording, sessionEnd bounded flush | 16 native observations delivered with stable IDs; last assistant event required a later no-user-prompt reopen/exit; task remains active until explicit end | Clean authenticated native start and resume pass on the same task, with both SDKs between prompts | Exact approved binding/bytes and native activation path observed; two SDK Skill events, none inferred from native activation | Both native prompts pass allowed recall and foreign-Session denial | No reinjection claim | 1.0.83 / gpt-5.6-luna on macOS arm64 | At `c86d5c6`: 18 unique events, zero duplicates on public-API replay, 16 Capture candidates, explicit end, reuse and valid audit through sequence 1179. Docker/Keycloak smoke passes. Local archive replay passes on Node 24/macOS arm64 and Docker Node 22/Linux arm64 and emulated x86_64. Native outage/compaction, other result shapes/platforms and execution from a published installation remain unqualified. See `docs/integrations/copilot-cli.md`. |
| Pi | No | No | No | No | None established | No | No | No conformance evidence | No registered conformance | None established | None | Absent from the authoritative registry; no contract, fixture, replay or live run is committed. |

**Harness status: Conditional for the three named verified setups; other clients
remain unqualified.** Promoting a row to verified requires a named client version, authentic frames,
deterministic replay and live public-API evidence for every applicable criterion
in `adapters/registry.json:10-20`; generated MCP configuration alone is not
verification.

## Context optimisation

| Area | Status | Severity | Evidence | Current test/monitor | Gap | Acceptance criteria | Suggested implementation slice | External dependency/owner decision |
|---|---|---|---|---|---|---|---|---|
| Current retrieval, provenance, freshness and graph bounds | Conditional | P1 | The bounded, PDP-filtered Knowledge planner remains the delivery path. CTX-8 adds an opt-in conservative mode with exact local `o200k_base` rendered-text counts when declared, labelled estimates otherwise, immutable source spans, explicit required revisions, shared Synveda allowance, preview and source/line hashes (`crates/synveda-gateway/src/context_api.rs`; `crates/synveda-retrieval/src/counting.rs`; `crates/synveda-retrieval/src/passage.rs`). The isolated 23-scenario product suite and CTX-8 demo pass in this source checkout; a combined gate tests checkpoint restart with required Knowledge in both governed optimisation modes. | `make eval-product`; `demos/ctx-8-context-optimisation.sh`; context-run/pack tests; planner-stage metrics | One fresh paired fixture measured 710 off versus 481 conservative local text tokens, 229 fewer; generated wrapper identifiers change absolute counts across runs. Provider request usage, host history, cache effects, cost and task success are unavailable. The four required-body tasks establish exact retention only. The combined gate retains an attributed checkpoint with room and omits it without losing required Knowledge under a tight exact budget. An isolated hostname handoff passed and was restored; the product image now uses its pinned installed Rust toolchain, but official Cargo index/archive endpoints still time out on a repeat direct host probe and full Compose/browser acceptance has not completed; held-out task quality remains open. CTX-6 checkpoint/restart is separate. | Representative tasks retain critical facts and permissions under lower delivered text; source edits, revocation and conflicts fail closed; measured host usage and latency stay within an explicitly reviewed boundary before broader rollout. | Restore official Cargo index/archive egress, finish source deployment acceptance and held-out quality/security cases, then run CTX-6 live compact/restart probes. | A supported client hook, model usage/quality evidence and checkpoint retention policy remain external decisions. |
| Stable prefix and provider prompt-cache alignment | Not ready | P1 | No context-result or provider prompt-cache implementation appears in the runtime. Context blocks are freshly composed and content-hashed per run (`crates/synveda-gateway/src/context_api.rs:2010-2023`). | Tokens-per-context and eval token metrics only | No deterministic prefix contract, cache-control integration, cache hit/eviction telemetry or measured provider savings. Caching before authorisation would be unsafe. | A byte-stable authorised prefix is separated from volatile task/recent content; provider cache evidence is measured; any local cache key includes tenant, principal-permission digest, project, Configuration version, Knowledge head, index generation, model and harness; PDP filtering precedes insertion. | Specify/render the stable prefix as a pure tested function, then trial provider cache controls behind one measured adapter. Do not add a shared result cache first. | Provider/harness must expose cache semantics and trustworthy usage data. |
| Task state, recent window, checkpoints and context offload | Conditional | P1 | CTX-6 now derives a typed, immutable checkpoint from admitted redacted Session events after Claude `PreCompact` spools a local boundary. Compact `SessionStart` requests bounded source-labelled checkpoint/recent-event context beside current Knowledge (`crates/synveda-gateway/src/sessions/checkpoint.rs`; `adapters/claude-code/src/turn.mts`; ADR-0119). Exact-role replay covers two boundaries, concurrent duplicate delivery, a missing source and a late tail; direct capture-candidate, scoped retention-disposal and required-Knowledge budget probes pass. The create/read/use/capture Session policy matrix passes under allow, revocation and foreign-tenant comparisons. An exact-role combined gate checks required Knowledge priority and checkpoint source attribution in both off and conservative modes. Four separate synthetic restart task pairs retain 12/12 declared Session facts with checkpoint/tail attribution and four exact required Knowledge bodies under a 1,400-token local budget; a warmed four-sample assisted preview p95 passed the predeclared 500 ms guard. The Claude mock-hook suite passes. | `demos/ctx-6-checkpoint-restart.sh`; Session gateway tests; Claude hook replay | The method retains labelled user excerpts, not a semantic task summary. Live proprietary-client compact/resume, model-observed task outcomes and provider usage remain open. The four local latency samples are a regression guard, not a production distribution. No fetch/pin/offload tool contract is claimed. | Restart preserves independently checked critical facts under a bounded shared budget with exact provenance, policy and source-removal behavior; live task probes quantify omissions and latency. | Keep the deterministic method opt-in at the supported Claude hook, complete a bounded live task probe, then decide whether any richer summary or offload protocol is warranted. | Approved synthetic live-client fixture, bounded provider spend, an isolated Compose handoff and source-build egress are needed for the remaining acceptance. |
| Progressive Skill/Tool disclosure | Conditional | P1 | Configuration independently narrows Skill/Tool advertisement (`crates/synveda-types/src/configuration.rs:342-350`); exact Skill versions and Tool bindings are separately governed. Claude syncs Skills on SessionStart (`adapters/claude-code/hooks/hooks.json:3-20`). | Skill/Tool gateway tests; Claude skill tests; context tests | No cross-harness measured metadata/body/on-demand disclosure policy, Tool body fetch protocol or activation outcome. Advertising every visible item has no production token/cost study. | Metadata tier is bounded and exact-versioned; bodies/files/tools are fetched only on demand with fresh PDP; disclosure reduces tokens without reducing task success; unsupported clients make no claim. | Add disclosure metrics and one Claude experiment using current exact version APIs before changing shared contracts. | Harness capabilities and activation callbacks are external constraints. |
| Model routing, passage selection and outcome/cost measurement | Not ready | P1 | Capture supports deterministic, Claude and vLLM extractors through the shared worker configuration boundary (`crates/synveda-gateway/src/runtime_config.rs`; `crates/synveda-ingest/src/extraction/`), and Configuration allowlists provider families (`crates/synveda-types/src/configuration.rs:410-426`). Eval distinguishes deterministic, BGE-M3 and credentialed model tiers. | `make eval`; `make eval-retrieval`; optional live extraction/LongMemEval targets | No production routing policy by task/risk/cost, extractive-before-summary pipeline, billing-grade prompt-cache savings or cost-per-useful-run dashboard. Model extraction remains credential-dependent, and subscription-client spend is not observable. | Routing decisions name exact model/configuration and stay within provider allowlist; passage-before-summary preserves provenance; labelled evaluations report helpfulness, correction, latency and actual provider usage where available; estimates are explicit. | Add offline routing experiments to the evaluation harness first; promote only a measured closed vocabulary through governed Configuration. | Requires model credentials/endpoints, provider usage fields and product-defined quality/cost thresholds. |

## P0/P1 gap register

These entries are implementation slices, not substitute feature IDs. Before
work begins, each maps to the named existing feature or one new catalogue/
backlog/STATUS entry. The evidence and current gap are the corresponding area
rows above.

| ID / feature | Severity | Exact scope and architecture seam | Non-goals | Acceptance tests | Rollout and rollback | Dependencies / owner decision |
|---|---|---|---|---|---|---|
| PR-01 / release artifact parity | P0 | Qualify optional CNPG deployment and fresh public v0.4.3 installation against published image/chart digests; test independent reproducibility. | No product-profile branch or chart redesign. | Pull the signed v0.4.3 inventory, images and chart anonymously, complete login/context on an empty host, boot the optional CNPG mode in a disposable cluster, and verify source/digest parity. | Keep v0.4.0-v0.4.3 tag/image/chart coordinates immutable; retain prior atomic manifests/charts for rollback. | Disposable cluster with the declared CNPG operator, fresh reference host and registry retention. |
| PR-02 / key custody acceptance | P0 | Exercise the existing Helm Secret/local-KMS seam through key use, rotation, restart and joint DB/key restore. | No cloud KMS/HSM implementation until its provider/custody decision is accepted. | Correct key restores and opens data; missing, stale and wrong keys fail closed; rendered artifacts/logs contain no plaintext. | Rehearse on isolated restored data; keep the prior KEK/envelopes until verification completes and document reversal. | Security owner chooses custody, delivery, backup and rotation ceremony. |
| PR-03 / OPS-5 | P0 | Add encrypted CNPG base backup/WAL archive, isolated PITR restore and post-restore schema/Knowledge/audit/index verification. | No multi-region failover or invented RPO/RTO. | Restore to chosen points before/after writes, verify frozen audit prefix and rebuilt indexes, and measure recurring RPO/RTO. | Enable on a non-production cluster, validate restore, then stage retention; rollback disables new scheduling without deleting valid backups. | Object store/region, retention, key custody, RPO/RTO and drill owner. |
| PR-04 / OPS-7 + drain | P1 | Persist login handoff, provide cross-process PDP/entity invalidation, withdraw readiness on termination and join/release workers within a bound. | No distributed cache product, second queue plane or multi-worker scale claim without evidence. | Three replicas finish cross-pod login, see an authority mutation within the bound, survive pod loss, and drain in-flight requests/leases on SIGTERM. | Shadow invalidation lag first; retain one-replica/Recreate settings until the complete test passes, with rollback to that shape. | Availability/staleness/drain SLO and invalidation/leadership choice. |
| PR-05 / capture reliability | P1 | Extend the existing capture-batch lease with persisted next-attempt/terminal state, jittered bounded retry and provider concurrency limits. | No Temporal or second queue plane. | Repeated failures do not storm; two workers/restarts produce one effect; terminal work is inspectable/retryable; queue age is bounded and alerted. | Deploy retry state before enabling new scheduling; rollback readers must tolerate the added state and safely retain leases. | Provider quotas, retry age/count and manual retry policy. |
| PR-06 / AUTH-6 | P1 | Add session inventory/revoke and issuer/token-identifier revocation at the existing credential verification seam. | No proprietary IdP or device-binding design without owner requirements. | Revocation takes effect within the declared bound, refresh replay fails, disable remains immediate and every action is audited. | Start with observe-only revocation telemetry, then enforce; rollback preserves records but can disable the new check only during incident recovery. | IdP claims/revocation behaviour and security-owner bound. |
| PR-07 / rate limits and quotas | P1 | Bound authenticated request frequency/concurrency and durable tenant usage at the gateway boundary without hot metric identity labels. | No billing/plans product or denied-resource counts. | Fair tenant/principal isolation under adversarial load, stable 429 envelopes, bounded DB/provider work and audited quota mutation. | Shadow-count before enforcement; enable per operation/tenant class and retain an audited emergency override. | Product quota semantics and distributed limiter storage. |
| PR-08 / TEN-5 | P1 | Build a restart-safe tenant lifecycle over the epoch-3 FK/table inventory: suspend/resume, streaming export/import and verified erasure. | Deployment reset is not tenant erasure; no legal-hold semantics without policy. | A populated tenant suspends, restores into clean storage and erases to zero tenant references with content-free certificate; interruption resumes. | Dry-run inventory first; hold source data until restore verification, and make destructive execution force-explicit/non-reversible. | Privacy/legal retention, hold and certificate decisions. |
| PR-09 / AUD-3 + AUD-4 | P1 | Feed canonical frozen-head audit export through a durable idempotent outbox to object lock and a documented SIEM schema. | No historical Cedar replay or content-bearing audit payload. | Object-locked prefix verifies offline and after DB restore; duplicate/retry delivery is harmless; lag/failure alerts fire. | Dual-write to non-authoritative sink first; rollback stops delivery without mutating the canonical chain. | WORM/SIEM targets, retention and compliance owner. |
| PR-10 / operational signals | P1 | Define bounded SLIs/alerts/runbooks and tenant-safe aggregate health APIs for session, context, Knowledge, registry, provider, backup and release state. | No full internal SaaS console or high-cardinality tenant labels on hot metrics. | Synthetic failures page with runbook/release marker; customer views distinguish loading/partial/stale/degraded without denied counts. | Ship recording rules/dashboards dark, tune from evidence, then alert; rollback alerts without removing raw signals. | Telemetry backend, on-call, regions, retention and SLOs. |
| PR-11 / signed supply chain | P1 | Add SBOM, identity-bound signing/provenance, immutable action/image references and vulnerability policy to release CI. | No hand-rolled crypto or silent baseline acceptance. | Installer verifies artifacts; every chart/image/archive has matching SBOM/attestation; critical findings and expired exceptions fail release. | Sign candidate artifacts before enforcing verification; retain last trusted manifest and documented key revocation. | Publisher identity, signing custody/notarisation and vulnerability SLA. |
| PR-12 / OPS-6 | P1 | Define N-1 compatibility and exercise data/key-preserving installed-host and Helm upgrade/rollback ordering. | No compatibility shim for deleted pre-1.0 epochs. | N-1 upgrades to N, failed N restores/rolls back within the window, and incompatible binaries refuse before traffic. | Canary restored data first; keep previous application artifact and measured restore path until the window closes. | Supported release window, outage budget and first post-1.0 migration policy. |
| PR-13 / licence and platform policy | P1 | Apache-2.0 is selected and repository/SDK metadata is aligned. Verify licence/notice carriage in the remaining release artifacts and publish the tested OS/arch/Postgres/Kubernetes window. | Preserve third-party terms and claim only tested platforms. | Release contents/metadata agree; every claimed platform has native build/install evidence and lifecycle dates. | Keep source/SDK licence evidence separate from unverified released archives; expand support only after measured acceptance. | Support owner, registry access and CI platforms. |
| PR-14 / CTX-7 + EVAL-6 | P1 | Stabilise dense query planning, publish the supported corpus/concurrency envelope and measure context/event/capture/pool/index/storage behaviour. | No benchmark-baseline rewrite to accept regressions. | Representative mixed tenants report p50/p95/p99, throughput, pool wait, plans, lag, growth and soak failures against reviewed thresholds. | Run as non-gating evidence, set thresholds from reviewed runs, then gate; rollback the code regression rather than the floor. | Hardware/model/corpus envelope and performance budget. |
| PR-15 / outcome and cost evidence | P1 | Join exact context feedback, delivered tokens/latency and provider-reported usage where available into the evaluation report. | Subscription estimates are not billing; quality may not be traded away silently. | Workloads report helpfulness/correction and actual or explicitly estimated cost per useful run; provenance and PDP filtering remain exact. | Evaluate offline first; change governed selection only after a measured result and revert by binding the prior Configuration version. | Provider usage fields and product definition of useful outcome. |
| PR-17 / CTX-6 + cache alignment | P1 | Add a versioned provenance-bound checkpoint and measured stable-prefix experiment at one real harness boundary. | No shared result cache, summary without evidence, or cache before PDP. | Compaction/restart preserves facts/corrections under a bounded window; any cache key includes all authority/version dimensions named above and measured quality does not regress. | Trial behind one governed Configuration version; rollback binds the prior version and retains checkpoint evidence. | Harness compact/restart hook, provider cache semantics and retention/sensitivity decision. |
| PR-18 / MEM-7 | P1 | Make federated identity uniqueness issuer-qualified through login, directory projection, principal scope and grant resolution, or enforce one issuer per tenant at admission. | No heuristic email linking or silent merge of existing principals. | Two issuers with the same `sub` cannot share scope/grants/Knowledge; link/unlink, revoke, relogin and directory tests retain exact authority and audit evidence. | Hard-cut before external production data, or deploy the one-issuer admission refusal first; rollback must never merge principals. | Multi-issuer product decision, issuer canonicalisation and owner-approved recovery/link proof. |
| PR-19 / Kubernetes boundary | P1 | Require a production TLS termination contract, disable unused ServiceAccount token automount and add default-deny ingress/egress NetworkPolicies around the existing Helm workload. | No service mesh mandate or invented Kubernetes API dependency. | Production render and kind probes complete login/context through declared dependencies while HTTP/plaintext, Kubernetes credential access and undeclared egress are refused. | Add as an opt-in hardened profile, verify dependency allowlists, then make it the supported production shape; rollback restores the prior chart without claiming production readiness. | Ingress/certificate owner, DNS/IdP/TEI/OTLP/directory endpoint inventory and cluster CNI support. |

P0 closure is necessary but not sufficient for a changed verdict. Reassess the
P1 register after the release, restore and key-plane acceptance suites run
together against the same production-shaped artifact set.
