{
  "name": "@synveda/claude-code-adapter",
  "version": "0.2.0",
  "private": true,
  "description": "Claude Code plugin: SessionStart inject + Stop/PreCompact/SessionEnd observe (ADPT-1, ADR-0027)",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test --test-timeout=60000 \"dist/*.test.mjs\""
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
